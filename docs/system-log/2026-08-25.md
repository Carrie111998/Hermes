# 2026-08-25

## 2026-08-25 — Point blocked ollama-url cron jobs at named litellm_proxy

**Repos touched:** `dizhaky/hermes-agent`

`scripts/fix-cron-ollama-base-url.py` no longer hardcodes `custom` + `http://localhost:4000/v1`. On mfc1 that port is closed; LiteLLM listens on Tailscale `:4010` and requires `LITELLM_MASTER_KEY`. The script now reads `custom_providers.litellm_proxy` from Hermes config (named provider + configured host so the safety-gate host-match and stored key both work) and falls back to the old localhost pair only when that provider is absent.

Live apply (mfc1 `~/.hermes/cron/jobs.json`) is the remaining operator step after merge; five target job IDs are unchanged.
