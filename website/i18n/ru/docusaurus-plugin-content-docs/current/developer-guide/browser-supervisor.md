---
sidebar_position: 18
title: Супервизор CDP браузера
description: Как Hermes обнаруживает собственные диалоги JS и реагирует на них, а
  также взаимодействует с iframe из разных источников через постоянное соединение
  CDP.
---

# Супервизор CDP браузера

Супервизор CDP закрывает два давних пробела в инструментах браузера Hermes:

1. **Встроенные диалоговые окна JS** (`alert`/`confirm`/`prompt`/`beforeunload`) блокируют
   JS-поток страницы. Без надзора агент не имеет возможности узнать
   Диалог открыт — последующие вызовы инструмента зависают или выдают непрозрачные ошибки.
2. **Интерфейсы iframe с перекрестным происхождением (OOPIF)** невидимы для верхнего уровня.
   `Runtime.evaluate`. Агент может видеть узлы iframe в снимке DOM, но
   не могу щелкнуть, ввести или просмотреть внутри них без подключенного сеанса CDP
   детская цель.

Супервизор решает обе проблемы, сохраняя постоянный WebSocket на бэкэнде.
Конечная точка CDP для каждой задачи браузера, отображение ожидающих диалогов и структура фрейма
в `browser_snapshot` и предоставляя инструмент `browser_dialog` для явного
ответы.

## Бэкэнд-поддержка

| Бэкэнд | Диалоговое обнаружение | Диалог ответа | Рамка дерева | OOPIF `Runtime.evaluate` через `browser_cdp(frame_id=...)` |
|---|---|---|---|---|
| Локальный Chrome (`--remote-debugging-port`) / `/browser connect` | ✓ | ✓ полный рабочий процесс | ✓ | ✓ |
| База браузеров | ✓ (через мост) | ✓ полный рабочий процесс (через мост) | ✓ | ✓ |
| Камофокс | ✗ нет CDP (только REST) ​​| ✗ | частично через снимок DOM | ✗ |

**Особенность браузерной базы.** Прокси-сервер CDP браузерной базы использует
автоматически закрывает собственные диалоги в течение примерно 10 мс, поэтому `Page.handleJavaScriptDialog`
не могу идти в ногу. Супервизор внедряет сценарий моста через
`Page.addScriptToEvaluateOnNewDocument`, который переопределяет
`window.alert`/`confirm`/`prompt` с синхронным XHR на магический хост
(`hermes-dialog-bridge.invalid`). `Fetch.enable` перехватывает эти XHR перед
они касаются сети — диалог становится событием `Fetch.requestPaused`,
супервизор фиксирует, а `respond_to_dialog` выполняет через
`Fetch.fulfillRequest` с телом JSON, которое декодирует внедренный скрипт.

С точки зрения страницы, `prompt()` по-прежнему возвращает предоставленный агентом
строка. С точки зрения агента это то же самое `browser_dialog(action=...)`
API в любом случае.

Camofox не поддерживается — нет поверхности CDP, только REST.

## Архитектура

### CDPSupervisor

Один `asyncio.Task` работает в фоновом потоке демона согласно Hermes `task_id`.
Удерживает постоянный WebSocket для конечной точки CDP серверной части. Поддерживает:

- **Очередь диалогов** — `List[PendingDialog]` с `{id, type, message, default_prompt, session_id, opened_at}`
- **Дерево фреймов** — `Dict[frame_id, FrameInfo]` с родительскими отношениями, URL-адресом, источником, дочерним сеансом с перекрестным происхождением.
- **Карта сеансов** — `Dict[session_id, SessionInfo]`, чтобы инструменты взаимодействия могли перенаправляться к нужному подключенному сеансу для операций OOPIF.
- **Последние ошибки консоли** — кольцевой буфер последних 50 для диагностики.

Подписка во вложении:

- `Page.enable` — `javascriptDialogOpening`, `frameAttached`, `frameNavigated`, `frameDetached`
- `Runtime.enable` — `executionContextCreated`, `consoleAPICalled`, `exceptionThrown`
- `Target.setAutoAttach {autoAttach: true, flatten: true}` — отображает дочерние цели OOPIF; супервизор включает `Page`+`Runtime` на каждом

Потокобезопасный доступ к состоянию через блокировку моментального снимка; обработчики инструментов (синхронизация) прочтите
замороженный снимок без ожидания.

### Жизненный цикл

- **Начало:** `SupervisorRegistry.get_or_start(task_id, cdp_url)` — вызывает
  `browser_navigate`, создание сеанса базы браузера, `/browser connect`.
  Идемпотент.
- **Стоп:** разрыв сеанса или `/browser disconnect`. Отменяет асинхронность
  задача, закрывает WebSocket, отбрасывает состояние.
- **Повторная привязка**: если URL-адрес CDP изменится (пользователь повторно подключается к новому Chrome),
  старый супервизор останавливается и запускается новый — состояние никогда не используется повторно
  через конечные точки.

### Политика диалога

Настраивается через `config.yaml` в `browser.dialog_policy`:

- **`must_respond`** (по умолчанию) — захват, поверхность в `browser_snapshot`, ожидание
  для явного вызова `browser_dialog(action=...)`. После 300-секундного тайм-аута безопасности
  без ответа, автоматическое отклонение и регистрация. Предотвращает зависание агента с ошибками
  навсегда.
- `auto_dismiss` — записать и немедленно закрыть; агент увидит это после
  факт через `browser_state` внутри `browser_snapshot`.
- `auto_accept` — записать и принять (полезно для `beforeunload`, где
  рабочий процесс хочет плавно уйти).

Политика индивидуальна для каждой задачи; нет переопределений для каждого диалога.

## Поверхность агента

### `browser_dialog` инструмент

```
browser_dialog(action, prompt_text=None, dialog_id=None)
```

- `action="accept"` / `"dismiss"` → отвечает на указанный или единственный ожидающий диалог (обязательно)
- `prompt_text=...` → текст для ввода в диалоговое окно `prompt()`
- `dialog_id=...` → устранение неоднозначности, когда в очереди стоит несколько диалогов (редко)

Инструмент предназначен только для ответов. Агент читает ожидающие диалоги от `browser_snapshot`.
вывод перед вызовом.

### расширение `browser_snapshot`

Добавляет три дополнительных поля к существующему выводу снимка, когда супервизор
прилагается:

```json
{
  "pending_dialogs": [
    {"id": "d-1", "type": "alert", "message": "Hello", "opened_at": 1650000000.0}
  ],
  "recent_dialogs": [
    {"id": "d-1", "type": "alert", "message": "...", "opened_at": 1650000000.0,
     "closed_at": 1650000000.1, "closed_by": "remote"}
  ],
  "frame_tree": {
    "top": {"frame_id": "FRAME_A", "url": "https://example.com/", "origin": "https://example.com"},
    "children": [
      {"frame_id": "FRAME_B", "url": "about:srcdoc", "is_oopif": false},
      {"frame_id": "FRAME_C", "url": "https://ads.example.net/", "is_oopif": true, "session_id": "SID_C"}
    ],
    "truncated": false
  }
}
```

- **`pending_dialogs`** — диалоги, в настоящее время блокирующие поток JS страницы.
  Чтобы ответить, агент должен позвонить на номер `browser_dialog(action=...)`. Пусто включено
  Browserbase, потому что их прокси-сервер CDP автоматически отключается в течение ~ 10 мс.

- **`recent_dialogs`** — кольцевой буфер до 20 недавно закрытых диалогов с
  тег `closed_by`: `"agent"` (мы ответили), `"auto_policy"` (локальный
  auto_dismiss/auto_accept), `"watchdog"` (время ожидания must_respond достигнуто) или
  `"remote"` (браузер/серверная часть закрыла его, например, Browserbase). Это
  как агенты в Browserbase все еще получают представление о том, что произошло.

- **`frame_tree`** — структура кадра, включая дочерние элементы перекрестного происхождения (OOPIF).
  Ограничено 30 записями + глубина OOPIF 2 для ограничения размера снимка при большом количестве рекламы.
  страницы. `truncated: true` появляется при достижении пределов; агенты, нуждающиеся в
  полное дерево может использовать `browser_cdp` с `Page.getFrameTree`.

Ни для одного из них не появляется новая схема инструмента — агент считывает его снимок.
уже просит.

### Доступность

Обе поверхности выходят на `_browser_cdp_check` (супервизор может работать только тогда, когда CDP
конечная точка достижима). В сеансах Camofox/без бэкэнда диалоговый инструмент
скрыто, а в снимке новые поля отсутствуют — никакого раздувания схемы.

## Взаимодействие iframe между источниками

`browser_cdp(frame_id=...)` маршрутизирует вызовы CDP (в частности, `Runtime.evaluate`)
через уже подключенный WebSocket супервизора с использованием дочернего объекта OOPIF
`sessionId`. Агенты выбирают идентификаторы кадров из
`browser_snapshot.frame_tree.children[]` где `is_oopif=true` и передайте их
до `browser_cdp`. Для iframe того же происхождения (без выделенного сеанса CDP)
агент использует `contentWindow`/`contentDocument` с верхнего уровня
Вместо этого `Runtime.evaluate` — супервизор выдает ошибку, указывающую на это.
резервный вариант, когда `frame_id` принадлежит не-OOPIF.

В Browserbase это единственный надежный путь взаимодействия iframe —
Соединения CDP без сохранения состояния (открытые для вызова `browser_cdp`) достигают подписанного URL-адреса
истечения срока действия, в то время как долговременное соединение супервизора сохраняет действительный сеанс.

## Макет файла

- `tools/browser_supervisor.py` — `CDPSupervisor`, `SupervisorRegistry`, `PendingDialog`, `FrameInfo`
- `tools/browser_dialog_tool.py` — обработчик инструмента `browser_dialog`
- `tools/browser_tool.py` — `browser_navigate` старт-крюк, `browser_snapshot` слияние, `/browser connect` повторное присоединение, `_cleanup_browser_session` разборка
- `toolsets.py` — регистрирует `browser_dialog` в `browser`, `hermes-acp`, `hermes-api-server` и основных наборах инструментов (с учетом доступности CDP).
- `hermes_cli/config.py` — значения по умолчанию для `browser.dialog_policy` и `browser.dialog_timeout_s`.

## Нецели

- Обнаружение/взаимодействие с Camofox (пробел в восходящем направлении; отслеживается отдельно)
- Потоковая передача событий диалога/кадра пользователю в реальном времени (потребуются перехватчики шлюза)
- Сохранение истории диалогов между сеансами (только в памяти)
- Политики диалога для каждого iframe (агент может выразить это через `dialog_id`)
- Замена `browser_cdp` — он остается запасным выходом для длинного хвоста (файлы cookie, область просмотра, регулирование сети)

## Тестирование

Модульные тесты (`tests/tools/test_browser_supervisor.py`) используют асинхронный макет CDP.
сервер, который достаточно знает протокол, чтобы осуществлять все переходы между состояниями:
прикрепить, включить, навигация, активировать диалог, закрыть диалог, прикрепить/отсоединить фрейм,
присоединение дочерней цели, удаление сеанса. Реальный бэкэнд E2E (база браузера + локальный
Браузер семейства Chromium) выполняется вручную — выполните упражнение через `/browser connect` до
живите в браузере семейства Chromium и запустите описанные тестовые примеры диалога/фрейма
выше.