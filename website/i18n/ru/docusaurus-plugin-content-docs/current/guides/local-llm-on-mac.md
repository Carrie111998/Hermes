---
sidebar_position: 2
title: Запускайте локальные LLM на Mac
description: Настройте локальный OpenAI-совместимый сервер LLM на macOS с помощью
  llama.cpp или MLX, включая выбор модели, оптимизацию памяти и реальные тесты на
  Apple Silicon.
---

# Запустите локальные LLM на Mac

В этом руководстве вы узнаете, как запустить локальный сервер LLM на macOS с API-интерфейсом, совместимым с OpenAI. Вы получаете полную конфиденциальность, нулевые затраты на API и удивительно хорошую производительность на Apple Silicon.

Мы рассматриваем два бэкэнда:

| Бэкэнд | Установить | Лучшее в | Формат |
|---------|---------|---------|--------|
| **llama.cpp** | `brew install llama.cpp` | Самое быстрое время создания первого токена, квантованный KV-кэш для малой памяти | ГГУФ |
| **омлкс** | [omlx.ai](https://omlx.ai) | Самая быстрая генерация токенов, встроенная оптимизация Metal | MLX (защитные тензоры) |

Оба предоставляют OpenAI-совместимую конечную точку `/v1/chat/completions`. Гермес работает с любым из них — просто наведите его на `http://localhost:8080` или `http://localhost:8000`.

:::информация Только для Apple Silicon
Это руководство предназначено для компьютеров Mac с Apple Silicon (M1 и новее). Компьютеры Intel Mac будут работать с llama.cpp, но без ускорения графического процессора — ожидайте значительного снижения производительности.
:::

---

## Выбор модели

Для начала мы рекомендуем **Qwen3.5-9B** — это надежная модель рассуждения, которая удобно помещается в более чем 8 ГБ унифицированной памяти с квантованием.

| Вариант | Размер на диске | Требуется ОЗУ (контекст 128 КБ) | Бэкэнд |
|---------|-------------|---------------------------|---------|
| Qwen3.5-9B-Q4_K_M (ГГУФ) | 5,3 ГБ | ~10 ГБ с квантованным KV-кешем | лама.cpp |
| Qwen3.5-9B-mlx-lm-mxfp4 (MLX) | ~5 ГБ | ~12 ГБ | омлкс |

**Практическое правило памяти:** размер модели + кэш KV. Модель 9B Q4 занимает ~5 ГБ. Кэш KV в контексте 128 КБ с квантованием Q4 добавляет ~4-5 ГБ. При использовании кэша KV по умолчанию (f16) он увеличивается до ~ 16 ГБ. Флаги квантованного кэша KV в llama.cpp — ключевой трюк для систем с ограниченной памятью.

Для более крупных моделей (27B, 35B) вам потребуется более 32 ГБ единой памяти. 9B — лучшее место для машин с объемом памяти 8–16 ГБ.

---

## Вариант А: llama.cpp

llama.cpp — наиболее переносимая локальная среда выполнения LLM. В macOS он использует Metal для ускорения графического процессора «из коробки».

### Установить

```bash
brew install llama.cpp
```

Это дает вам глобальную команду `llama-server`.

### Загрузите модель

Вам нужна модель в формате GGUF. Самый простой источник — Hugging Face через `huggingface-cli`:

```bash
brew install huggingface-cli
```

Затем скачайте:

```bash
huggingface-cli download unsloth/Qwen3.5-9B-GGUF Qwen3.5-9B-Q4_K_M.gguf --local-dir ~/models
```

:::совет Закрытые модели
Некоторые модели Hugging Face требуют аутентификации. Если вы получаете ошибку 401 или 404, сначала запустите `huggingface-cli login`.
:::

### Запускаем сервер

```bash
llama-server -m ~/models/Qwen3.5-9B-Q4_K_M.gguf \
  -ngl 99 \
  -c 131072 \
  -np 1 \
  -fa on \
  --cache-type-k q4_0 \
  --cache-type-v q4_0 \
  --host 0.0.0.0
```

Вот что делает каждый флаг:

| Флаг | Цель |
|------|---------|
| `-ngl 99` | Выгрузите все слои в графический процессор (металл). Используйте большое число, чтобы ничего не оставалось в процессоре. |
| `-c 131072` | Размер окна контекста (128 000 токенов). Уменьшите это значение, если у вас мало памяти. |
| `-np 1` | Количество параллельных слотов. Оставьте значение 1 для однопользовательского использования — большее количество слотов разделит ваш бюджет памяти. |
| `-fa on` | Вспышка внимания. Уменьшает использование памяти и ускоряет вывод в длинном контексте. |
| `--cache-type-k q4_0` | Квантуйте ключевой кэш до 4-битного. **Это экономит память.** |
| `--cache-type-v q4_0` | Квантуйте кэш значений до 4-битного. Вместе с вышесказанным это сокращает кэш-память KV примерно на 75% по сравнению с f16. |
| `--host 0.0.0.0` | Слушайте на всех интерфейсах. Используйте `127.0.0.1`, если вам не нужен доступ к сети. |

Сервер готов, когда вы видите:

```
main: server is listening on http://0.0.0.0:8080
srv  update_slots: all slots are idle
```

### Оптимизация памяти для систем с ограниченными возможностями

Флаги `--cache-type-k q4_0 --cache-type-v q4_0` являются наиболее важной оптимизацией для систем с ограниченной памятью. Вот влияние в контексте 128K:

| Тип кэша KV | Кэш-память KV (128K ctx, модель 9B) |
|---------------|--------------------------------------|
| f16 (по умолчанию) | ~16 ГБ |
| q8_0 | ~8 ГБ |
| **q4_0** | **~4 ГБ** |

На Mac с 8 ГБ памяти используйте кэш `q4_0` KV и выберите модель меньшего размера, которая по-прежнему может соответствовать минимальному контексту Hermes 64 КБ. На 16 ГБ можно комфортно делать 128К контекста. На 32 ГБ+ вы можете использовать более крупные модели или несколько параллельных слотов.

Если вам по-прежнему не хватает памяти, сокращайте контекст только при сохранении минимума Hermes в 64 КБ или выше; в противном случае переключитесь на меньшую модель или меньшее квантование (Q3_K_M вместо Q4_K_M).

### Проверьте это

```bash
curl -s http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Qwen3.5-9B-Q4_K_M.gguf",
    "messages": [{"role": "user", "content": "Hello!"}],
    "max_tokens": 50
  }' | jq .choices[0].message.content
```

### Получить название модели

Если вы забыли имя модели, запросите конечную точку модели:

```bash
curl -s http://localhost:8080/v1/models | jq '.data[].id'
```

---

## Вариант Б: MLX через omlx

[omlx](https://omlx.ai) — это собственное приложение для macOS, которое управляет моделями MLX и обслуживает их. MLX — это собственная платформа машинного обучения Apple, оптимизированная специально для унифицированной архитектуры памяти Apple Silicon.

### Установить

Загрузите и установите с сайта [omlx.ai](https://omlx.ai). Он предоставляет графический интерфейс для управления моделями и встроенный сервер.

### Загрузите модель

Используйте приложение OMLX для просмотра и загрузки моделей. Найдите `Qwen3.5-9B-mlx-lm-mxfp4` и загрузите его. Модели хранятся локально (обычно в `~/.omlx/models/`).

### Запускаем сервер

Omlx по умолчанию обслуживает модели на `http://127.0.0.1:8000`. Начните обслуживание из пользовательского интерфейса приложения или используйте интерфейс командной строки, если он доступен.

### Проверьте это

```bash
curl -s http://127.0.0.1:8000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Qwen3.5-9B-mlx-lm-mxfp4",
    "messages": [{"role": "user", "content": "Hello!"}],
    "max_tokens": 50
  }' | jq .choices[0].message.content
```

### Список доступных моделей

Omlx может обслуживать несколько моделей одновременно:

```bash
curl -s http://127.0.0.1:8000/v1/models | jq '.data[].id'
```

---

## Тесты: llama.cpp против MLX

Оба серверных модуля протестированы на одной и той же машине (Apple M5 Max, унифицированная память 128 ГБ) с одной и той же моделью (Qwen3.5-9B) с сопоставимыми уровнями квантования (Q4_K_M для GGUF, mxfp4 для MLX). Пять различных запросов, по три запуска каждое, серверные части тестируются последовательно, чтобы избежать конкуренции за ресурсы.

### Результаты

| Метрическая | llama.cpp (Q4_K_M) | MLX (mxfp4) | Победитель |
|--------|-------------------|-------------|--------|
| **TTFT (среднее)** | **67 мс** | 289 мс | llama.cpp (в 4,3 раза быстрее) |
| **ТТФТ (стр.50)** | **66 мс** | 286 мс | llama.cpp (в 4,3 раза быстрее) |
| **Генерация (среднее)** | 70 ток/с | **96 ток/с** | MLX (на 37% быстрее) |
| **Поколение (стр.50)** | 70 ток/с | **96 ток/с** | MLX (на 37% быстрее) |
| **Общее время (512 токенов)** | 7,3 с | **5,5 с** | MLX (на 25 % быстрее) |

### Что это значит

- **llama.cpp** превосходно справляется с быстрой обработкой — его флэш-внимание + конвейер квантованного кэша KV дает вам первый токен примерно за 66 мс. Если вы создаете интерактивные приложения, для которых важна скорость реагирования (чат-боты, автозаполнение), это значительное преимущество.

- **MLX** генерирует токены примерно на 37 % быстрее после запуска. Для пакетных рабочих нагрузок, создания длинных форм или любых задач, где общее время выполнения имеет большее значение, чем начальная задержка, MLX завершается раньше.

- Оба бэкэнда **чрезвычайно согласованы** — разница между запусками была незначительной. Вы можете положиться на эти цифры.

### Какой выбрать?

| Вариант использования | Рекомендация |
|----------|---------------|
| Интерактивный чат, инструменты с низкой задержкой | лама.cpp |
| Производство сортового проката, массовая обработка | MLX (omlx) |
| С ограничениями по памяти (8–16 ГБ) | llama.cpp (квантованный KV-кэш не имеет аналогов) |
| Одновременное обслуживание нескольких моделей | omlx (встроенная поддержка нескольких моделей) |
| Максимальная совместимость (в том числе и с Linux) | лама.cpp |

---

## Подключитесь к Гермесу

Как только ваш локальный сервер заработает:

```bash
hermes model
```

Выберите **Пользовательская конечная точка** и следуйте инструкциям. Он запросит базовый URL-адрес и название модели — используйте значения из любого бэкэнда, который вы настроили выше.

---

## Тайм-ауты

Hermes автоматически обнаруживает локальные конечные точки (локальный хост, IP-адреса локальной сети) и уменьшает тайм-ауты потоковой передачи. Для большинства настроек не требуется настройка.

Если вы по-прежнему сталкиваетесь с ошибками таймаута (например, очень большие контексты на медленном оборудовании), вы можете переопределить таймаут потокового чтения:

```bash
# In your .env — raise from the 120s default to 30 minutes
HERMES_STREAM_READ_TIMEOUT=1800
```

| Тайм-аут | По умолчанию | Локальная автонастройка | Переопределить переменную Env |
|---------|---------|----------------------|------------------|
| Потоковое чтение (уровень сокета) | 120-е | Поднят до 1800-х годов | `HERMES_STREAM_READ_TIMEOUT` |
| Обнаружение устаревшего потока | 180-е годы | Полностью отключен | `HERMES_STREAM_STALE_TIMEOUT` |
| Вызов API (без потоковой передачи) | 1800-е годы | Никаких изменений не требуется | `HERMES_API_TIMEOUT` |

Тайм-аут чтения потока чаще всего вызывает проблемы — это крайний срок на уровне сокета для получения следующего фрагмента данных. Во время предварительного заполнения больших контекстов локальные модели могут не выдавать выходные данные в течение нескольких минут во время обработки запроса. Автоматическое обнаружение решает эту проблему прозрачно.

:::tip Первый поворот без звука обычно представляет собой предварительное заполнение, а не зависание.
Hermes отправляет системное приглашение и схемы инструментов при каждом вызове, поэтому на более медленном оборудовании первый поворот может включать несколько минут тишины, пока модель обрабатывает это приглашение, прежде чем что-либо генерировать. Это предварительное заполнение на работе, а не остановленный сеанс. См. [Медленный первый ответ (предварительное заполнение)](./local-ollama-setup.md#slow-first-response-prefill) в руководстве Ollama, чтобы узнать о мерах по снижению риска, таких как сохранение загрузки модели и обрезка фиксированного запроса с помощью `hermes prompt-size`.
:::