---
sidebar_position: 4
title: MCP (Протокол контекста модели)
description: Подключите агент Hermes к внешним серверам инструментов через MCP и точно
  контролируйте, какие инструменты MCP загружает Hermes.
---

# MCP (Протокол контекста модели)

MCP позволяет агенту Hermes подключаться к внешним серверам инструментов, чтобы агент мог использовать инструменты, находящиеся вне самого Hermes — GitHub, базы данных, файловые системы, стеки браузеров, внутренние API и многое другое.

Если вы когда-нибудь хотели, чтобы Hermes использовал инструмент, который уже существует где-то еще, обычно MCP — самый простой способ сделать это.

:::tip Из Клода Кода?
Блок `mcpServers` в вашем `~/.claude.json` сопоставляется с `mcp_servers` в `config.yaml` Hermes — и `hermes import-agent claude-code` автоматически переносит его (вместе с навыками и инструкциями). См. [Импорт из других агентов](../import-from-other-agents.md).
:::

## Что дает вам MCP

- Доступ к внешним экосистемам инструментов без предварительного написания собственного инструмента Hermes.
— Локальные серверы stdio и удаленные серверы HTTP MCP в одной конфигурации.
- Автоматическое обнаружение и регистрация инструмента при запуске
- Оболочки утилит для ресурсов MCP и подсказки, если они поддерживаются сервером.
- Фильтрация для каждого сервера, поэтому вы можете предоставлять только те инструменты MCP, которые действительно хотите, чтобы Hermes видел.

## Быстрый старт

1. Поддержка MCP поставляется со стандартной установкой — никаких дополнительных действий не требуется.

2. Добавьте сервер MCP в `~/.hermes/config.yaml`:

```yaml
mcp_servers:
  filesystem:
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-filesystem", "/home/user/projects"]
```

3. Запустите Гермес:

```bash
hermes chat
```

4. Попросите Hermes использовать возможности, поддерживаемые MCP.

Например:

```text
List the files in /home/user/projects and summarize the repo structure.
```

Hermes обнаружит инструменты сервера MCP и будет использовать их как любой другой инструмент.

## Каталог: установка в один клик для одобренных Nous MCP

Hermes отправляет тщательно подобранный каталог серверов MCP, проверенный сотрудниками Nous.
и слились. По умолчанию они отключены — устанавливайте только то, что вам действительно нужно.
хочу.

```bash
hermes mcp                # interactive picker (default)
hermes mcp catalog        # plain-text list, scriptable
hermes mcp install n8n    # install a catalog entry by name
```

Средство выбора показывает каждую запись с ее текущим статусом:

```
n8n          available              Manage and inspect n8n workflows from Hermes
linear       enabled                Linear issue/project management (remote OAuth)
github       installed (disabled)   GitHub repo + PR tools
```

Нажмите `Enter` в строке, чтобы установить (и пройти все необходимые учетные данные),
включить, отключить или удалить. Записи каталога хранятся под
`optional-mcps/` в репозитории hermes-agent — присутствие в этом каталоге означает
Нус одобрение. Не существует уровня представления сообщества; записи добавляются
объединение PR.

Для записей каталога могут потребоваться:

- **Ключ API** — Hermes запрашивает во время установки и записывает значение в
  `~/.hermes/.env`. Несекретные значения (базовые URL-адреса) сохраняются в том же файле.
- **OAuth** (удаленный MCP) — в вашей конфигурации записан как `auth: oauth`; MCP
  клиент открывает браузер при первом подключении.
- **OAuth** (сторонний поставщик, например Google/GitHub) — Гермес указывает на
  `hermes auth <provider>`, если вы еще не прошли аутентификацию.

### Выбор инструмента во время установки

После настройки учетных данных Hermes проверяет сервер MCP, чтобы составить список всех
инструмент, который он предоставляет и представляет контрольный список:

```
Select tools for 'linear' (SPACE toggle, ENTER confirm)
  [x] find_issues       Find issues matching a query
  [x] get_issue         Get a single issue
  [x] create_issue      Create a new issue
  [ ] delete_workspace  Delete a Linear workspace
  ...
```

Предварительно проверенные строки берутся из:

1. **Ваш предыдущий выбор**, если вы уже устанавливали эту запись ранее (переустанавливает
   сохраните то, что у вас было — значения по умолчанию манифеста не отменяют их)
2. **Манифест `tools.default_enabled`**, если запись объявляет один (некоторые
   записи каталога предварительно удаляют мутирующие или редко полезные инструменты)
3. **Все**, если ни то, ни другое не применимо.

Некоторые записи с очень большими автоматически созданными поверхностями (например, `cloudflare`,
~3300 инструментов конечных точек OpenAPI) вместо этого объявите `tools.default_excluded` —
кураторский черный список имен и шаблонов glob. Установка одного из этих скипов
чек-лист целиком и пишет `tools.exclude`; все не совпадает
остается включенным, включая инструменты, которые сервер добавляет позже. Редактировать
`mcp_servers.<name>.tools.exclude` в config.yaml, чтобы повторно включить семейство.

Отправьте контрольный список, нажав ENTER. В папку попадают только проверенные инструменты.
`mcp_servers.<name>.tools.include`. Если выбрать все, фильтр не будет работать.
написано (чистейшая форма конфига, идентичное поведение).

**Если проверка не удалась** (сервер недоступен, OAuth еще не завершен,
служба резервного копирования не запущена), установка по-прежнему завершается успешно: манифест
`tools.default_enabled` применяется напрямую (если объявлено) или фильтр не применяется.
написано (если нет). Перезапустите `hermes mcp configure <name>`, как только сервер
доступен для уточнения.

### Модель доверия

Установка записи каталога выполняется так, как указано в манифесте — `git clone`,
команды `bootstrap` записи (`pip install`, `npm install` и т. д.) и
в конечном итоге собственный код сервера MCP. Манифесты проходят PR-анализ.
репозиторий Hermes-Agent, поэтому Ноус просматривал каждую запись перед ее отправкой —
**но вам все равно следует прочитать манифест перед установкой**, особенно
репозиторий поля `source:`, команды `install.bootstrap:` и любые
`transport.command:` вызов.

Манифесты живут по адресу
[`optional-mcps/<name>/manifest.yaml`](https://github.com/NousResearch/hermes-agent/tree/main/optional-mcps)
на Гитхабе. Средство выбора также печатает URL-адрес `source:` манифеста при установке.
время, чтобы вы могли быстро проверить исходный репозиторий. MCP веб-панели
страница отображает одну и ту же информацию для каждой записи каталога — транспорт, тип аутентификации,
URL-адрес конечной точки (HTTP) или команда + аргументы (stdio), источник/ссылка установки git и
команды начальной загрузки и примечания по настройке — с `source:`, отображаемым как
кликабельная ссылка, позволяющая точно узнать, с чем именно связана или запускается запись.
прежде чем нажать «Установить».

### Совместимость версий манифеста

Манифесты закрепляют `manifest_version`. Каталог совместим с предыдущими версиями: если
PR добавляет запись с более новым `manifest_version`, чем установленный у вас Hermes.
понимает, сборщик выдаст предупреждение (`⚠ '<name>' требует более новой версии
Обновление Hermes`) for that entry instead of silently hiding it. Run `hermes`
установить последнюю версию Гермеса, когда увидите это.

### Замена `${ENV_VAR}` во время выполнения

Внутри записи `transport.command`, `transport.args`, `transport.url`,
и заполнители `headers`, `${VAR}` разрешаются во время подключения к серверу.
из переменных среды (которые включают в себя все, что есть в `~/.hermes/.env`).
Это полезно, когда запись каталога хочет ссылаться на значение, которое пользователь
настроено в другом месте — например. `${HOME}/foo` или `${MY_PROVIDER_TOKEN}`.

Также заменяются контекстные переменные в стиле курсора (с учетом регистра):
`${userHome}` (домашний каталог), `${workspaceFolder}` (рабочая область сеанса).
корень), `${workspaceFolderBasename}` и `${pathSeparator}` / `${/}`
(разделитель пути ОС). См.
[Справочник по конфигурации MCP](/docs/reference/mcp-config-reference) для получения подробной информации.

Обратите внимание, что это отличается от `${INSTALL_DIR}` в манифестах каталога, который
заменяется во время установки путем, в котором каталог клонировал запись
репо в.

### Обновление выбора инструмента позже

```bash
hermes mcp configure linear
```

Повторно открывает тот же контрольный список с предварительно проверенным текущим выбором. Используйте это
когда вы хотите включить больше инструментов или когда сервер добавил новые инструменты, которые
вы хотите принять участие.

### Обновление манифеста каталога

MCP никогда не обновляются автоматически. Повторно запустите `hermes mcp install <name>`, чтобы обновить
после обновления Hermes, если изменилась версия манифеста.

Чтобы добавить МКП в каталог, откройте PR на
[`optional-mcps/`](https://github.com/NousResearch/hermes-agent/tree/main/optional-mcps).

### Метаданные предложения (`suggest:`)

В манифесте может быть объявлен необязательный блок `suggest:` с `keywords:` и/или
`hosts:` списков. Поверхности пользовательского интерфейса (в настоящее время композитор приложения для настольных компьютеров) используют его для
предложите «Добавить &lt;сервер&gt;» одним щелчком мыши. таблетку, когда в вашем черновике упоминается один из
ключевые слова как завершенное слово или содержат вставленную ссылку, имя хоста которой заканчивается
с одним из суффиксов хоста. Это чисто рекомендательный момент — установки все еще идут.
через одни и те же проверенные пути каталога/конфигурации — и большинство размещенных удаленных
записи (Atlassian, Sentry, Notion, Stripe, Vercel, Supabase и другие)
заявить об этом.

GitHub намеренно **нет** в каталоге: для его размещенного MCP требуется каждый
клиент для создания собственного приложения OAuth (обычная динамическая регистрация клиента
отклонено), а комплексные навыки Гермеса `github/*`, управляющие интерфейсом командной строки `gh`, являются
более эффективная интеграция. На рабочем столе GitHub вместо этого упоминает предложение
`github-auth` навык, когда `gh` еще не вошел в систему.

## Два типа MCP-серверов

### Стдио-серверы

Серверы Stdio работают как локальные подпроцессы и обмениваются данными через стандартный ввод/вывод.

```yaml
mcp_servers:
  github:
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-github"]
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: "***"
```

Используйте stdio-серверы, когда:
- сервер установлен локально
- вам нужен доступ к локальным ресурсам с низкой задержкой
- вы следуете документации сервера MCP, в которой указаны `command`, `args` и `env`.

### HTTP-серверы

Серверы HTTP MCP — это удаленные конечные точки, к которым Hermes подключается напрямую.

```yaml
mcp_servers:
  remote_api:
    url: "https://mcp.example.com/mcp"
    headers:
      Authorization: "Bearer ***"
```

Используйте HTTP-серверы, когда:
- сервер MCP размещен в другом месте
- ваша организация предоставляет внутренние конечные точки MCP
- вы не хотите, чтобы Hermes создавал локальный подпроцесс для этой интеграции

### HTTP-серверы с аутентификацией OAuth

Большинству размещенных серверов MCP (Cloudflare, Linear, Sentry, Atlassian, Asana, Figma, Stripe и т. д.) требуется OAuth 2.1 вместо статического токена носителя. Установите `auth: oauth`, и Hermes будет выполнять обнаружение, идентификацию клиента, PKCE, обмен токенами, обновление и поэтапную аутентификацию через MCP Python SDK.

Hermes идентифицирует себя с помощью [Документа метаданных идентификатора клиента](../../reference/mcp-config-reference.md#client-identification-cimd-and-dcr) на серверах, которые его поддерживают, и возвращается к динамической регистрации клиентов на тех, которые этого не делают. Оба являются автоматическими; там нечего настраивать.

:::совет Figma удаленный MCP
Размещенная конечная точка Figma (`https://mcp.figma.com/mcp`) вносит в разрешенный список динамическую регистрацию клиентов по **точному `client_name`** — пустые `"Hermes Agent"` 403, а `"Claude Code"` и `"Codex"` успешны. Hermes автоматически устанавливает `oauth.client_name: "Claude Code"` для `mcp.figma.com`, поэтому установка и вход в систему работают без особых хитростей:

```yaml
mcp_servers:
  figma:
    url: "https://mcp.figma.com/mcp"
    auth: oauth
```

Или: `hermes mcp install figma`, затем `hermes mcp login figma`.
:::

```yaml
mcp_servers:
  linear:
    url: "https://mcp.linear.app/mcp"
    auth: oauth
```

При первом подключении Hermes печатает URL-адрес авторизации, открывает ваш браузер, если это возможно, и ожидает обратного вызова OAuth на локальном порту обратной связи. Токены кэшируются по адресу `~/.hermes/mcp-tokens/<server>.json` с разрешениями 0o600; последующие запуски повторно используют их автоматически, пока обновление не завершится неудачей.

**Удаленные/автономные хосты.** Когда Hermes работает на другом компьютере, чем ваш браузер, обратный вызов не может достичь вашего ноутбука. Два способа завершения потока:

- **Вставка обратно (без настройки):** на интерактивном терминале Hermes печатает «Или вставьте сюда URL-адрес перенаправления…» рядом с URL-адресом авторизации. Откройте URL-адрес в своем браузере, подтвердите, скопируйте полный URL-адрес, на который попадает браузер (при перенаправлении будет отображаться ошибка подключения — это ожидаемо), вставьте его в командную строку. Голые строки запроса `?code=…&state=…` тоже работают.
- **Перенаправление порта SSH:** `ssh -N -L <port>:127.0.0.1:<port> user@host` в отдельный терминал, затем дайте перенаправлению пройти нормально.
- **Обратный вызов через прокси (`redirect_uri`):** когда общедоступная конечная точка HTTPS перенаправляет на хост (например, воронку Tailscale или обратный прокси-сервер, указывающий на порт обратного вызова), установите `oauth.redirect_uri`, и перенаправление браузера достигнет Hermes самостоятельно — туннель или вставка не требуются:

```yaml
mcp_servers:
  myserver:
    url: "https://mcp.example.com/mcp"
    auth: oauth
    oauth:
      redirect_port: 8765                                # fixed port for the proxy to target
      redirect_uri: "https://oauth.example.ts.net/callback"
```

Для полностью автономных шлюзов (бот для обмена сообщениями, отсутствие интерактивного терминала вообще) дополнительный [`mcp-oauth-remote-gateway` навык](../skills/optional/mcp/mcp-mcp-oauth-remote-gateway.md) помогает агенту завершить поток вручную и записать токены там, где их ожидает Hermes.

**Подводный камень: WAF отклоняет URI перенаправления `127.0.0.1`.** Некоторые провайдеры передают свой сервер авторизации с помощью WAF, который 403s обрабатывает любой запрос на авторизацию, строка запроса которого содержит литерал `127.0.0.1` (известный пример — шлюз AWS API Reclaim.ai — каждая попытка возвращает `{"message":"Forbidden"}` до достижения приложения OAuth). Установите `oauth.redirect_host: localhost` вместо использования `http://localhost:<port>/callback`; прослушиватель обратного вызова в любом случае все равно связывает `127.0.0.1`.

См. [OAuth через SSH / удаленные хосты](../../guides/oauth-over-ssh.md#mcp-servers) для полного пошагового руководства, включая серверы без DCR (например, Slack), предварительно зарегистрированные `client_id`/`client_secret`, настройку области и повторную аутентификацию через `hermes mcp login <server>`.

**Подводная ошибка — поставщики, которые не поддерживают автоматическую регистрацию (Google Диск, Atlassian).** Некоторые серверы отклоняют этап динамической регистрации клиента (RFC 7591), на котором основан `auth: oauth` — официальный сервер Google Drive (`https://drivemcp.googleapis.com/mcp/v1`) возвращает `400 Bad Request`, поэтому клиент OAuth не создается и токен не получается. Признак незаметен: эти серверы также обслуживают `tools/list` *без* аутентификации, поэтому `hermes mcp login` может перечислить инструменты и выглядеть так, как будто они работают, но каждый реальный вызов инструмента позже истекает по тайм-ауту. `hermes mcp login` теперь обнаруживает это (он проверяет, действительно ли токен попал на диск) и предлагает вам предоставить собственный клиент OAuth. Создайте его в консоли провайдера и добавьте в конфиг:

```yaml
mcp_servers:
  googledrive:
    url: "https://drivemcp.googleapis.com/mcp/v1"
    auth: oauth
    oauth:
      client_id: "<your-oauth-client-id>"
      client_secret: "<your-oauth-client-secret>"
```

Затем запустите `hermes mcp login googledrive` — с предварительно зарегистрированным клиентом Hermes пропускает регистрацию и запускает обычный процесс авторизации в браузере.

**Подводный камень — гонка автоматической перезагрузки конфигурации.** Когда вы редактируете `~/.hermes/config.yaml` внутри работающего сеанса Hermes, CLI автоматически перезагружает соединения MCP с тайм-аутом 30 секунд. Этого недостаточно для интерактивного потока OAuth. Добавьте запись, затем запустите `hermes mcp login <server>` с нового терминала — он ждет полные 5 минут, пока вы завершите аутентификацию.

## mTLS/клиентские сертификаты

Удаленные HTTP-серверы MCP, которым требуется взаимный TLS (аутентификация по сертификату клиента), поддерживаются через `client_cert`/`client_key`. Hermes передает разрешенный сертификат базовому HTTP-клиенту для подтверждения TLS.

`client_cert` принимает три формы:

- **Единый комбинированный путь PEM** — один файл, содержащий сертификат и закрытый ключ:

```yaml
mcp_servers:
  internal_api:
    url: "https://mcp.internal.example.com/mcp"
    client_cert: "~/.certs/mcp-client.pem"
```

- **A `[cert, key]` 2-tuple** — сертификат и ключ в отдельных файлах (эквивалент настройки `client_cert` + `client_key`):

```yaml
mcp_servers:
  internal_api:
    url: "https://mcp.internal.example.com/mcp"
    client_cert: ["~/.certs/mcp-client.crt", "~/.certs/mcp-client.key"]
```

- **3-кортеж `[cert, key, password]`** — когда закрытый ключ зашифрован, третий элемент является ключевой фразой:

```yaml
mcp_servers:
  internal_api:
    url: "https://mcp.internal.example.com/mcp"
    client_cert: ["~/.certs/mcp-client.crt", "~/.certs/mcp-client.key", "${MCP_KEY_PASSWORD}"]
```

Вы также можете полностью разделить сертификат и ключ через `client_cert` (комбинированный PEM) плюс явный `client_key`. Пути поддерживают расширение `~`; отсутствующий файл вызывает явную ошибку на уровне сервера, а не непрозрачный сбой подтверждения TLS.

## Заголовок идентификатора пользователя

Удаленные серверы HTTP/SSE MCP, которые определяют поведение идентификатора вызывающего абонента (ограничения скорости для каждого пользователя, журналы аудита, многопользовательскую маршрутизацию), могут отправлять заголовок идентификатора при каждом запросе через `identity_header`:

```yaml
mcp_servers:
  team_api:
    url: "https://mcp.team.example.com/mcp"
    identity_header:
      name: "X-User-Id"
      value_from: "static"   # "static" (default) or "profile"
      value: "alice"         # required for static
```

- `value_from: static` отправляет литерал `value` из config.yaml.
- `value_from: profile` отправляет активное имя профиля Hermes, разрешенное один раз во время соединения. Это полезно, когда несколько профилей на одном компьютере взаимодействуют с одним и тем же сервером и необходимо различить их.

Явная запись в сопоставлении `headers` сервера с тем же именем (любой регистр) всегда имеет преимущество; заголовок идентификации никогда не переопределяет вашу собственную конфигурацию заголовка. О недействительных блоках `identity_header` предупреждаются и игнорируются — они никогда не блокируют подключение сервера. На серверах stdio ключ игнорируется с предупреждением (транспорты stdio не имеют заголовков).

## Справочник по базовой конфигурации

Hermes читает конфигурацию MCP из `~/.hermes/config.yaml` под `mcp_servers`.

### Общие ключи

| Ключ | Тип | Значение |
|---|---|---|
| `command` | строка | Исполняемый файл для stdio MCP-сервера |
| `args` | список | Аргументы в пользу сервера stdio |
| `env` | картографирование | Переменные среды, передаваемые на сервер stdio |
| `url` | строка | Конечная точка HTTP MCP |
| `headers` | картографирование | HTTP-заголовки для удаленных серверов |
| `client_cert` | строка \| список | Сертификат клиента для mTLS — комбинированный путь PEM, или `[cert, key]`/`[cert, key, password]` |
| `client_key` | строка | Путь PEM к личному ключу клиента (если он отделен от `client_cert`) |
| `identity_header` | картографирование | Необязательный заголовок идентификатора пользователя для серверов HTTP/SSE — `{name, value_from: static\|profile, value}` |
| `timeout` | номер | Тайм-аут вызова инструмента |
| `connect_timeout` | номер | Тайм-аут начального соединения (также ограничивает рукопожатие MCP `initialize`) |
| `idle_timeout_seconds` | номер | Перезапустите сервер stdio по истечении этого количества секунд без вызова инструмента (`0` = никогда, по умолчанию). Сервер прозрачно перезапускается при следующем вызове инструмента. |
| `max_lifetime_seconds` | номер | Перезапустите сервер stdio по истечении этого общего срока службы (`0` = никогда, по умолчанию). Прозрачный перезапуск при следующем использовании. |
| `enabled` | бул | Если `false`, Hermes полностью пропускает сервер |
| `supports_parallel_tool_calls` | бул | Если `true`, инструменты с этого сервера могут работать одновременно |
| `tools` | картографирование | Политика фильтрации и утилит для отдельных серверов |

### Минимальный пример stdio

```yaml
mcp_servers:
  filesystem:
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-filesystem", "/tmp"]
```

### Перезагрузка stdio-серверов с большим объемом памяти

Серверы MCP на основе браузера (например, `@playwright/mcp`) поддерживают полную версию Chromium.
резидент после первого вызова инструмента — сотни МБ, которые никогда не будут получены
выпущен. Включите автоматическую переработку, и после этого сервер будет отключен.
предел простоя/времени жизни, а затем прозрачно перезапускается в следующий раз, когда один из
его инструменты вызываются (его инструменты все время остаются зарегистрированными):

```yaml
mcp_servers:
  playwright:
    command: "npx"
    args: ["-y", "@playwright/mcp@latest", "--headless"]
    idle_timeout_seconds: 900     # recycle after 15 min without a tool call
    max_lifetime_seconds: 86400   # and at least once a day regardless
```

### Минимальный пример HTTP

```yaml
mcp_servers:
  company_api:
    url: "https://mcp.internal.example.com"
    headers:
      Authorization: "Bearer ***"
```

## Встроенные пресеты

Для известных серверов MCP `hermes mcp add` принимает флаг `--preset`, который заполняет детали транспорта, поэтому вам не нужно искать команду и аргументы. Предустановка предоставляет только значения по умолчанию — все остальное (переменные окружения, заголовки, фильтрация), которые вы передаете в той же командной строке, по-прежнему имеет преимущество.

| Предустановленные | Что это подключается |
|---|---|
| `codex` | Сервер MCP Codex CLI (`codex mcp-server` через stdio). Требуется интерфейс командной строки `codex` в PATH. |

```bash
# Add Codex CLI as an MCP server in one line
hermes mcp add codex --preset codex
```

Это записывает эквивалент:

```yaml
mcp_servers:
  codex:
    command: "codex"
    args: ["mcp-server"]
```

Вы можете выбрать любое локальное имя (подойдет `hermes mcp add my-codex --preset codex`); предустановка предоставляет только значения по умолчанию `command`/`args`.

## Как Hermes регистрирует инструменты MCP

Hermes добавляет к инструментам MCP префиксы, чтобы они не конфликтовали со встроенными именами:

```text
mcp_<server_name>_<tool_name>
```

Примеры:

| Сервер | инструмент MCP | Зарегистрированное имя |
|---|---|---|
| `filesystem` | `read_file` | `mcp_filesystem_read_file` |
| `github` | `create-issue` | `mcp_github_create_issue` |
| `my-api` | `query.data` | `mcp_my_api_query_data` |

На практике обычно не требуется вызывать имя префикса вручную — Гермес видит инструмент и выбирает его в ходе обычных рассуждений.

### Дезинфекция результатов инструмента и `_meta`

К каждому результату инструмента MCP применяются два поведения, прежде чем модель его увидит:

- **Невидимые символы TAG Unicode удаляются.** Символы в диапазоне U+E0000–U+E007F не отображаются в терминалах и пользовательском интерфейсе чата, но полностью видны модели — классический канал контрабанды с быстрым внедрением для вредоносного или скомпрометированного сервера. Hermes удаляет их из результатов работы инструментов, содержимого ресурсов и описаний инструментов. Легитимные последовательности тегов эмодзи (региональные флаги, такие как 🏴 Football Football, например 🏴 Football Football, Football Football) сохраняются.
- **Обнаружен продавец `_meta`; ключи, зарезервированные протоколом, не являются.** Когда сервер прикрепляет сопоставление `_meta` к результату инструмента (пространства имен поставщика, такие как `com.example/handoff`), Hermes передает его в модель вместе с содержимым результата. Ключи с префиксами, зарезервированными протоколом: метка `modelcontextprotocol` или `mcp`, за которой следует другая метка, например `modelcontextprotocol.io/...` или `tools.mcp.com/...` — удаляются, что соответствует правилам для имен ключей спецификации MCP. Если ничего обращенного к модели не осталось, поле `_meta` полностью опускается.

## Утилиты MCP

При поддержке Hermes также регистрирует служебные инструменты вокруг ресурсов MCP и предлагает:

- `list_resources`
- `read_resource`
- `list_prompts`
- `get_prompt`

Они регистрируются для каждого сервера с одинаковым шаблоном префикса, например:

- `mcp_github_list_resources`
- `mcp_github_get_prompt`

### Важно

Эти служебные инструменты теперь учитывают возможности:
- Hermes регистрирует утилиты ресурсов только в том случае, если сеанс MCP действительно поддерживает операции с ресурсами.
- Hermes регистрирует утилиты подсказок только в том случае, если сеанс MCP действительно поддерживает операции подсказок.

Таким образом, сервер, который предоставляет вызываемые инструменты, но не имеет ресурсов/подсказок, не получит эти дополнительные оболочки.

## Фильтрация по серверам

Вы можете контролировать, какие инструменты каждый сервер MCP предоставляет Hermes, что позволяет более детально управлять пространством имен ваших инструментов.

### Полностью отключить сервер

```yaml
mcp_servers:
  legacy:
    url: "https://mcp.legacy.internal"
    enabled: false
```

Если `enabled: false`, Hermes полностью пропускает сервер и даже не пытается установить соединение.

### Инструменты сервера белого списка

```yaml
mcp_servers:
  github:
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-github"]
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: "***"
    tools:
      include: [create_issue, list_issues]
```

Зарегистрированы только эти серверные инструменты MCP.

Записи в `include`/`exclude` также могут быть шаблонами `*`, `?`, `[...]`,
сопоставлены с учетом регистра): `include: ["*_dns_*"]` регистрирует каждый инструмент,
имя содержит `_dns_`. Обычные записи без метасимволов сохраняют точное соответствие.
Globs — это практичный способ фильтрации серверов, которые предоставляют тысячи
автоматически создаваемые инструменты для конечных точек по семействам продуктов.

### Инструменты сервера черного списка

```yaml
mcp_servers:
  stripe:
    url: "https://mcp.stripe.com"
    tools:
      exclude: [delete_customer]
```

Зарегистрированы все серверные инструменты, кроме исключенных.

### Шаблоны глобусов

Оба списка принимают глобусы в стиле fnmatch наряду с точными именами, что важно для
огромные плоские поверхности, такие как API MCP Cloudflare (`?codemode=false`, ~3300
инструменты), где исключение продуктовых областей по одной конечной точке за раз непрактично:

```yaml
mcp_servers:
  cloudflare:
    url: "https://mcp.cloudflare.com/mcp?codemode=false"
    auth: oauth
    tools:
      exclude: ["*_radar_*", "*_accounts_dlp_*", "*_zones_web3_*"]
```

Записи без метасимволов glob (`*`, `?`, `[`) точно совпадают — `docs`
исключает только инструмент с именем `docs` и никогда `docs_search`.

### Правило приоритета

Если присутствуют оба:

```yaml
tools:
  include: [create_issue]
  exclude: [create_issue, delete_issue]
```

`include` побеждает.

### Инструменты фильтрации также

Вы также можете отдельно отключить обертки утилит, добавленных Hermes:

```yaml
mcp_servers:
  docs:
    url: "https://mcp.docs.example.com"
    tools:
      prompts: false
      resources: false
```

Это означает:
- `tools.resources: false` отключает `list_resources` и `read_resource`.
- `tools.prompts: false` отключает `list_prompts` и `get_prompt`.

### Полный пример

```yaml
mcp_servers:
  github:
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-github"]
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: "***"
    tools:
      include: [create_issue, list_issues, search_code]
      prompts: false

  stripe:
    url: "https://mcp.stripe.com"
    headers:
      Authorization: "Bearer ***"
    tools:
      exclude: [delete_customer]
      resources: false

  legacy:
    url: "https://mcp.legacy.internal"
    enabled: false
```

## Что произойдет, если все отфильтровать?

Если ваша конфигурация отфильтровывает все вызываемые инструменты и отключает или опускает все поддерживаемые утилиты, Hermes не создает пустой набор инструментов MCP во время выполнения для этого сервера.

Это сохраняет список инструментов чистым.

## Поведение во время выполнения

### Время обнаружения

Hermes обнаруживает серверы MCP при запуске и регистрирует их инструменты в обычном реестре инструментов.

### Динамическое обнаружение инструментов

Серверы MCP могут уведомлять Hermes об изменении доступных инструментов во время выполнения, отправляя уведомление `notifications/tools/list_changed`. Когда Hermes получает это уведомление, он автоматически повторно получает список инструментов сервера и обновляет реестр — `/reload-mcp` вручную не требуется.

Это полезно для серверов MCP, возможности которых изменяются динамически (например, сервер, который добавляет инструменты при загрузке новой схемы базы данных или удаляет инструменты, когда служба переходит в автономный режим).

Обновление защищено блокировкой, поэтому быстрые уведомления с одного и того же сервера не вызывают перекрывающихся обновлений. Уведомления о подсказках и изменениях ресурсов (`prompts/list_changed`, `resources/list_changed`) получены, но еще не обработаны.

### Перезагрузка

Если вы меняете конфигурацию MCP, используйте:

```text
/reload-mcp
```

Это перезагрузит серверы MCP из конфигурации и обновит список доступных инструментов. Информацию об изменениях инструментов во время выполнения, вносимых самим сервером, см. в разделе [Динамическое обнаружение инструментов](#dynamic-tool-discovery) выше.

### Наборы инструментов

Каждый настроенный сервер MCP также создает набор инструментов среды выполнения, если он предоставляет хотя бы один зарегистрированный инструмент:

```text
mcp-<server>
```

Это упрощает анализ серверов MCP на уровне набора инструментов.

## Модель безопасности

### Фильтрация окружения Stdio

Для серверов stdio Hermes не передает вслепую вашу полную среду оболочки.

Передаются только явно настроенные `env` плюс безопасный базовый уровень. Это снижает случайную утечку секрета.

### Контроль экспозиции на уровне конфигурации

Новая поддержка фильтрации также является средством контроля безопасности:
- отключите опасные инструменты, которые вы не хотите, чтобы модель видела
- предоставлять только минимальный белый список для конфиденциального сервера
- отключите оболочки ресурсов/подсказок, если вы не хотите, чтобы эта поверхность была открыта

## Примеры использования

### Сервер GitHub с минимальной поверхностью управления проблемами

```yaml
mcp_servers:
  github:
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-github"]
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: "***"
    tools:
      include: [list_issues, create_issue, update_issue]
      prompts: false
      resources: false
```

Используйте его как:

```text
Show me open issues labeled bug, then draft a new issue for the flaky MCP reconnection behavior.
```

### Сервер Stripe с удаленными опасными действиями

```yaml
mcp_servers:
  stripe:
    url: "https://mcp.stripe.com"
    headers:
      Authorization: "Bearer ***"
    tools:
      exclude: [delete_customer, refund_payment]
```

Используйте его как:

```text
Look up the last 10 failed payments and summarize common failure reasons.
```

### Сервер файловой системы для одного корня проекта

```yaml
mcp_servers:
  project_fs:
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-filesystem", "/home/user/my-project"]
```

Используйте его как:

```text
Inspect the project root and explain the directory layout.
```

## Устранение неполадок

### Сервер MCP не подключается

Проверьте:

```bash
# Verify MCP deps are installed (already included in standard install)
cd ~/.hermes/hermes-agent && uv pip install -e ".[mcp]"

node --version
npx --version
```

Затем проверьте свою конфигурацию и перезапустите Hermes.

### Инструменты не отображаются

Возможные причины:
- сервер не смог подключиться
- открытие не удалось
- ваша конфигурация фильтра исключила инструменты
- на этом сервере не существует возможности утилиты
- сервер отключен с помощью `enabled: false`

Если вы намеренно фильтруете, это ожидаемо.

### Почему не появились утилиты ресурсов и подсказок?

Потому что теперь Hermes регистрирует эти оболочки только в том случае, если оба они верны:
1. ваша конфигурация позволяет это
2. сеанс сервера действительно поддерживает эту возможность

Это сделано намеренно и обеспечивает честность списка инструментов.

## Параллельные вызовы инструментов

По умолчанию инструменты MCP запускаются последовательно — по одному. Если ваш сервер MCP предоставляет инструменты, которые можно безопасно запускать одновременно (например, запросы только для чтения, независимые вызовы API), вы можете выбрать параллельное выполнение:

```yaml
mcp_servers:
  docs:
    command: "docs-server"
    supports_parallel_tool_calls: true
```

Если `supports_parallel_tool_calls` равен `true`, Hermes может одновременно запускать несколько инструментов с этого сервера в рамках одного пакета вызова инструментов, как это происходит со встроенными инструментами только для чтения (web_search, read_file и т. д.).

:::осторожно
Включайте параллельные вызовы только для серверов MCP, инструменты которых можно безопасно запускать одновременно. Если инструменты читают и записывают общее состояние, файлы, базы данных или внешние ресурсы, просмотрите условия гонки чтения/записи, прежде чем включать этот параметр.
:::

## Поддержка выборки MCP

Серверы MCP могут запрашивать вывод LLM у Hermes через протокол `sampling/createMessage`. Это позволяет серверу MCP попросить Hermes сгенерировать текст от его имени, что полезно для серверов, которым необходимы возможности LLM, но которые не имеют собственного доступа к модели.

Выборка **включена по умолчанию** для всех серверов MCP (если MCP SDK поддерживает ее). Настройте его для каждого сервера под ключом `sampling`:

```yaml
mcp_servers:
  my_server:
    command: "my-mcp-server"
    sampling:
      enabled: true            # Enable sampling (default: true)
      model: "openai/gpt-4o"  # Override model for sampling requests (optional)
      max_tokens_cap: 4096     # Max tokens per sampling response (default: 4096)
      timeout: 30              # Timeout in seconds per request (default: 30)
      max_rpm: 10              # Rate limit: max requests per minute (default: 10)
      max_tool_rounds: 5       # Max tool-use rounds in sampling loops (default: 5)
      allowed_models: []       # Allowlist of model names the server may request (empty = any)
      log_level: "info"        # Audit log level: debug, info, or warning (default: info)
```

Обработчик выборки включает в себя ограничитель скорости скользящего окна, тайм-ауты для каждого запроса и ограничения глубины инструментального цикла для предотвращения неконтролируемого использования. Метрики (количество запросов, ошибки, используемые токены) отслеживаются для каждого экземпляра сервера.

Чтобы отключить выборку для определенного сервера:

```yaml
mcp_servers:
  untrusted_server:
    url: "https://mcp.example.com"
    sampling:
      enabled: false
```

## Поддержка выявления MCP

Серверы MCP могут запрашивать у пользователя структурированный ввод в середине вызова инструмента через протокол `elicitation/create` (mcp Python SDK ≥ 1.11.0). Hermes направляет запросы в **режиме формы** через свою существующую поверхность утверждения — интерактивную подсказку в CLI/TUI или кнопки подтверждения на шлюзовых платформах, таких как Telegram и Slack, — поэтому запрос доходит до вас, где бы ни находился сеанс. Вызовы **URL-режима** (когда сервер указывает на внешний URL-адрес) отклоняются как неподдерживаемые.

Выявление **включено по умолчанию** для каждого сервера. Настройте его под ключом `elicitation`:

```yaml
mcp_servers:
  my_server:
    command: "my-mcp-server"
    elicitation:
      enabled: true    # default: true
      timeout: 300     # seconds to wait for your answer (default: 300)
```

Тайм-аут по умолчанию, равный 5 минутам, отражает значение по умолчанию для утверждения шлюза, поэтому пользователи на асинхронных поверхностях имеют время ответить до того, как сервер сдастся. Метрики для каждого сервера (запросы, принятые, отклоненные, ошибки) отслеживаются в обработчике.

## Запуск Hermes в качестве сервера MCP

Помимо подключения **к** серверам MCP, Hermes также может **быть** сервером MCP. Это позволяет другим агентам с поддержкой MCP (Claude Code, Cursor, Codex или любому клиенту MCP) использовать возможности обмена сообщениями Hermes — составлять списки разговоров, читать историю сообщений и отправлять сообщения на все подключенные платформы.

### Когда это использовать

- Вы хотите, чтобы Claude Code, Cursor или другой агент кодирования отправлял и читал сообщения Telegram/Discord/Slack через Hermes.
- Вам нужен один сервер MCP, который одновременно соединяет все подключенные платформы обмена сообщениями Hermes.
- У вас уже есть работающий шлюз Hermes с подключенными платформами.

### Быстрый старт

```bash
hermes mcp serve
```

Это запускает сервер stdio MCP. Клиент MCP (а не вы) управляет жизненным циклом процесса.

### Конфигурация клиента MCP

Добавьте Hermes в конфигурацию вашего клиента MCP. Например, в `~/.claude/claude_desktop_config.json` Клода Кода:

```json
{
  "mcpServers": {
    "hermes": {
      "command": "hermes",
      "args": ["mcp", "serve"]
    }
  }
}
```

Или, если вы установили Hermes в определенное место:

```json
{
  "mcpServers": {
    "hermes": {
      "command": "/home/user/.hermes/hermes-agent/venv/bin/hermes",
      "args": ["mcp", "serve"]
    }
  }
}
```

### Доступные инструменты

Сервер MCP предоставляет 10 инструментов, соответствующих поверхности моста каналов OpenClaw, а также браузеру каналов, специфичному для Hermes:

| Инструмент | Описание |
|------|-------------|
| `conversations_list` | Список активных диалогов обмена сообщениями. Фильтруйте по платформе или ищите по названию. |
| `conversation_get` | Получите подробную информацию об одном разговоре по ключу сеанса. |
| `messages_read` | Прочитайте историю недавних сообщений для разговора. |
| `attachments_fetch` | Извлекайте нетекстовые вложения (изображения, мультимедиа) из конкретного сообщения. |
| `events_poll` | Опрос новых событий разговора с момента позиции курсора. |
| `events_wait` | Длительный опрос/блокировка до прихода следующего события (почти в реальном времени). |
| `messages_send` | Отправьте сообщение через платформу (например, `telegram:123456`, `discord:#general`). |
| `channels_list` | Перечислите доступные цели обмена сообщениями на всех платформах. |
| `permissions_list_open` | Список ожидающих запросов на утверждение, наблюдаемых во время этого сеанса моста. |
| `permissions_respond` | Разрешите или отклоните ожидающий запрос на утверждение. |

### Система событий

Сервер MCP включает в себя мост живых событий, который опрашивает базу данных сеансов Hermes на наличие новых сообщений. Это дает клиентам MCP информацию о входящих разговорах практически в реальном времени:

```
# Poll for new events (non-blocking)
events_poll(after_cursor=0)

# Wait for next event (blocks up to timeout)
events_wait(after_cursor=42, timeout_ms=30000)
```

Типы событий: `message`, `approval_requested`, `approval_resolved`.

Очередь событий находится в памяти и запускается при подключении моста. Более старые сообщения доступны через `messages_read`.

### Опции

```bash
hermes mcp serve              # Normal mode
hermes mcp serve --verbose    # Debug logging on stderr
```

### Как это работает

Сервер MCP считывает данные разговоров непосредственно из хранилища сеансов Hermes: `~/.hermes/state.db` является основным источником, а `sessions.json` сохраняется только как устаревший резервный вариант. Фоновый поток опрашивает базу данных на наличие новых сообщений и поддерживает очередь событий в памяти. Для отправки сообщений он использует тот же внутренний механизм отправки (`tools/send_message_tool.py`), который обеспечивает доставку cron, и `hermes send` CLI.

Шлюз НЕ обязательно должен быть запущен для операций чтения (список разговоров, история чтения, события опроса). Он ДЕЙСТВИТЕЛЬНО должен быть запущен для операций отправки, поскольку адаптерам платформы необходимы активные соединения.

### Текущие лимиты

- Сегодня встроенный `hermes mcp serve` предоставляет сервер MCP **только для stdio**. Если вам нужен сервер HTTP MCP, запустите отдельный адаптер — или, что гораздо чаще, используйте **клиентскую** сторону MCP Hermes, которая уже поддерживает как stdio, так и HTTP (`url` + `headers` в `mcp_servers.yaml` / `config.yaml`; см. [HTTP-серверы](#http-servers) выше).
- Опрос событий с интервалом ~ 200 мс с помощью опроса БД, оптимизированного по времени (пропускает работу, если файлы не изменены)
- Протокола push-уведомлений `claude/channel` пока нет.
- Отправка только текстовых сообщений (без отправки мультимедиа/вложений через `messages_send`)

## Связанные документы

- [Использовать MCP с Hermes](/guides/use-mcp-with-hermes)
- [Команды CLI](/reference/cli-commands)
- [Команды слэша](/reference/slash-commands)
- [Часто задаваемые вопросы](/ссылка/часто задаваемые вопросы)