---
sidebar_position: 15
---

# Обратный вызов WeCom (собственное приложение)

Подключите Hermes к WeCom (Enterprise WeChat) как самостоятельно созданному корпоративному приложению, используя модель обратного вызова/веб-перехватчика.

:::info WeCom Bot против WeCom Callback
Hermes поддерживает два режима интеграции WeCom:
- **[WeCom Bot](wecom.md)** — стиль бота, подключается через WebSocket. Более простая настройка, работает в групповых чатах.
- **WeCom Callback** (эта страница) — собственное приложение, получающее зашифрованные обратные вызовы XML. Отображается как первоклассное приложение на боковой панели пользователей WeCom. Поддерживает маршрутизацию между несколькими корпорациями.
:::

См. также: [WeCom Bot](./wecom.md) для интеграции в стиле бота.

> Запустите `hermes gateway setup` и выберите **Обратный вызов WeCom** для пошагового руководства.

## Как это работает

1. Вы регистрируете самостоятельно созданное приложение в консоли администратора WeCom.
2. WeCom отправляет зашифрованный XML в конечную точку обратного вызова HTTP.
3. Гермес расшифровывает сообщение и ставит его в очередь для агента.
4. Немедленное подтверждение (без звука — пользователю ничего не отображается)
5. Агент обрабатывает запрос (обычно 3–30 минут).
6. Ответ доставляется заранее через API WeCom `message/send`.

## Предварительные условия

- Корпоративная учетная запись WeCom с доступом администратора.
- Пакеты Python `aiohttp` и `httpx` (включены в установку по умолчанию)
— Общедоступный сервер для URL-адреса обратного вызова (или туннель, например ngrok).

## Настройка

### 1. Создайте самостоятельно созданное приложение в WeCom

1. Перейдите в [Консоль администратора WeCom](https://work.weixin.qq.com/) → **Приложения** → **Создать приложение**.
2. Запишите свой **Корпоративный идентификатор** (отображается в верхней части консоли администратора).
3. В настройках приложения создайте **Корпоративную тайну**.
4. Обратите внимание на **Идентификатор агента** на странице обзора приложения.
5. В разделе **Получение сообщений** настройте URL-адрес обратного вызова:
   - URL: `http://YOUR_PUBLIC_IP:8645/wecom/callback`
   - Токен: сгенерируйте случайный токен (WeCom предоставляет его).
   - EncodingAESKey: сгенерируйте ключ (WeCom предоставляет его).

### 2. Настройка переменных среды

Добавьте в свой файл `.env`:

```bash
WECOM_CALLBACK_CORP_ID=your-corp-id
WECOM_CALLBACK_CORP_SECRET=your-corp-secret
WECOM_CALLBACK_AGENT_ID=1000002
WECOM_CALLBACK_TOKEN=your-callback-token
WECOM_CALLBACK_ENCODING_AES_KEY=your-43-char-aes-key

# Optional
# WECOM_CALLBACK_HOST=  # optional pin; unset = dual-stack (all interfaces, IPv4+IPv6)
WECOM_CALLBACK_PORT=8645
WECOM_CALLBACK_ALLOWED_USERS=user1,user2
```

### 3. Запустите шлюз

```bash
hermes gateway
```

(Используйте `hermes gateway start` только после того, как `hermes gateway install` зарегистрирует службу systemd/launchd.)

Адаптер обратного вызова запускает HTTP-сервер на настроенном порту. WeCom проверит URL-адрес обратного вызова с помощью запроса GET, а затем начнет отправлять сообщения через POST.

## Справочник по конфигурации

Установите их в `config.yaml` в `platforms.wecom_callback.extra` или используйте переменные среды:

| Настройка | По умолчанию | Описание |
|---------|---------|-------------|
| `corp_id` | — | Идентификатор корпорации WeCom Enterprise (обязательно) |
| `corp_secret` | — | Корпоративный секрет для самостоятельного приложения (обязательно) |
| `agent_id` | — | Идентификатор агента собственного приложения (обязательно) |
| `token` | — | Токен подтверждения обратного вызова (обязательно) |
| `encoding_aes_key` | — | 43-значный ключ AES для шифрования обратного вызова (обязательно) |
| `host` | не установлено (двойной стек: все интерфейсы, IPv4+IPv6) | Привязать адрес для сервера обратного вызова HTTP |
| `port` | `8645` | Порт для сервера обратного вызова HTTP |
| `path` | `/wecom/callback` | URL-путь для конечной точки обратного вызова |

## Маршрутизация нескольких приложений

Для предприятий, использующих несколько самостоятельно созданных приложений (например, в разных отделах или дочерних компаниях), настройте список `apps` в `config.yaml`:

```yaml
platforms:
  wecom_callback:
    enabled: true
    extra:
      host: "0.0.0.0"
      port: 8645
      apps:
        - name: "dept-a"
          corp_id: "ww_corp_a"
          corp_secret: "secret-a"
          agent_id: "1000002"
          token: "token-a"
          encoding_aes_key: "key-a-43-chars..."
        - name: "dept-b"
          corp_id: "ww_corp_b"
          corp_secret: "secret-b"
          agent_id: "1000003"
          token: "token-b"
          encoding_aes_key: "key-b-43-chars..."
```

Пользователи ограничиваются `corp_id:user_id`, чтобы предотвратить конфликты между корпорациями. Когда пользователь отправляет сообщение, адаптер записывает, к какому приложению (корпорации) он принадлежит, и направляет ответы через токен доступа правильного приложения.

## Контроль доступа

Ограничьте пользователей, которые могут взаимодействовать с приложением:

```bash
# Allowlist specific users
WECOM_CALLBACK_ALLOWED_USERS=zhangsan,lisi,wangwu

# Or allow all users
WECOM_CALLBACK_ALLOW_ALL_USERS=true
```

## Конечные точки

Адаптер предоставляет:

| Метод | Путь | Цель |
|--------|------|---------|
| ПОЛУЧИТЬ | `/wecom/callback` | Подтверждение URL-адреса (WeCom отправляет его во время установки) |
| ПОСТ | `/wecom/callback` | Обратный вызов зашифрованного сообщения (сюда WeCom отправляет сообщения пользователя) |
| ПОЛУЧИТЬ | `/health` | Проверка работоспособности — возвращает `{"status": "ok"}` |

## Шифрование

Все полезные данные обратного вызова шифруются с помощью AES-CBC с использованием EncodingAESKey. Адаптер обрабатывает:

- **Входящие**: расшифровка полезных данных XML, проверка подписи SHA1.
- **Исходящие**: ответы отправляются через упреждающий API (не зашифрованный ответ обратного вызова).

Реализация шифрования совместима с официальным SDK WXBizMsgCrypt от Tencent.

## Ограничения

- **Нет потоковой передачи** — ответы приходят в виде полных сообщений после завершения работы агента.
- **Нет индикаторов ввода** — модель обратного вызова не поддерживает статус ввода.
- **Только текст** — в настоящее время поддерживается ввод текстовых сообщений; ввод изображения/файла/голоса еще не реализован. Агент знает о возможностях исходящего мультимедиа через подсказку платформы WeCom (изображения, документы, видео, голос).
- **Задержка ответа** — сеансы агента занимают 3–30 минут; пользователи видят ответ после завершения обработки

## Устранение неполадок

**Не удалось проверить подпись.**
WeCom подписывает каждый запрос с помощью **токена**, который вы зарегистрировали в администраторе.
консоль. Несоответствие между токеном, настроенным в Hermes, и токеном,
Ожидается, что консоль администратора является наиболее распространенной причиной. Повторно скопируйте **Token** и
**EncodingAESKey** из консоли администратора — их легко обрезать. Пробелы
значения в `~/.hermes/.env` около `=` также нарушат проверку подписи. После
исправим, перезапустите `hermes gateway run`.

**URL обратного вызова недоступен / этап проверки не выполнен.**
WeCom обращается к общедоступному URL-адресу, который вы зарегистрировали. Подтвердите:
1. Ваш обратный прокси/туннель перенаправляет `/wecom/callback` на порт шлюза.
2. URL-адрес в консоли администратора — HTTPS (WeCom отклоняет простой HTTP).
3. Извне вашей сети: `curl -i https://<your-domain>/wecom/callback`
   возвращает нечто иное, чем таймаут (4xx без параметров запроса подойдет —
   это просто означает, что слушатель доступен).

**Порт недоступен/прослушиватель не привязан.**
Проверьте журналы `hermes gateway run` для связанного хоста/порта. Если адаптер привязан к
`127.0.0.1` вам необходимо использовать обратный прокси-сервер или туннель — серверы WeCom.
не могу связаться с шлейфом. Оставьте `extra.host` отключенным, чтобы использовать двойной стек по умолчанию.
примените привязку (все интерфейсы, IPv4+IPv6) или закрепите интерфейс в `config.yaml` (плюс
`allowed_source_cidrs` при прямом разоблачении) или сохраните петлю и используйте туннель.
например Cloudflare Tunnel/nginx.