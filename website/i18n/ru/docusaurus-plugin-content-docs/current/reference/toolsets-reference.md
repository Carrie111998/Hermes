---
sidebar_position: 4
title: Справочник по наборам инструментов
description: Справочник по основным, композитным, платформенным и динамическим наборам
  инструментов Hermes.
---

# Справочник по наборам инструментов

Наборы инструментов — это именованные пакеты инструментов, которые контролируют действия агента. Они являются основным механизмом настройки доступности инструментов для каждой платформы, для каждого сеанса или для каждой задачи.

## Как работают наборы инструментов

Каждый инструмент принадлежит ровно одному набору инструментов. Когда вы включаете набор инструментов, все инструменты в этом пакете становятся доступными агенту. Наборы инструментов бывают трёх видов:

- **Ядро** — единая логическая группа связанных инструментов (например, `file` пакеты `read_file`, `write_file`, `patch`, `search_files`).
- **Композитный** — объединяет несколько основных наборов инструментов для общего сценария (например, `debugging` объединяет файлы, терминалы и веб-инструменты).
- **Платформа** — полная конфигурация инструмента для конкретного контекста развертывания (например, `hermes-cli` используется по умолчанию для интерактивных сеансов CLI).

## Настройка наборов инструментов

### За сеанс (CLI)

```bash
hermes chat --toolsets web,file,terminal
hermes chat --toolsets debugging        # composite — expands to file + terminal + web
hermes chat --toolsets all              # everything
```

### Для каждой платформы (config.yaml)

```yaml
toolsets:
  - hermes-cli          # default for CLI
  # - hermes-telegram   # override for Telegram gateway
```

### Интерактивное управление

```bash
hermes tools                            # curses UI to enable/disable per platform
```

Или во время сессии:

```
/tools list
/tools disable browser
/tools enable homeassistant
```

## Основные наборы инструментов

| Набор инструментов | Инструменты | Цель |
|---------|-------|---------|
| `browser` | `browser_back`, `browser_cdp`, `browser_click`, `browser_console`, `browser_dialog`, `browser_get_images`, `browser_navigate`, `browser_press`, `browser_scroll`, `browser_snapshot`, `browser_type`, `browser_vision`, `web_search` | Автоматизация ядра браузера. Включает `web_search` в качестве запасного варианта для быстрого поиска. `browser_cdp` и `browser_dialog` контролируются во время выполнения — регистрируются только тогда, когда конечная точка CDP доступна в начале сеанса (через конфигурацию `/browser connect`, `browser.cdp_url`, Browserbase или Camofox). `browser_dialog` работает вместе с полями `pending_dialogs` и `frame_tree`, которые `browser_snapshot` добавляет при подключении супервизора CDP. |
| `clarify` | `clarify` | Задайте пользователю вопрос, когда агенту требуются разъяснения. |
| `code_execution` | `execute_code` | Запускайте сценарии Python, которые программно вызывают инструменты Hermes. |
| `coding` | составной (`file` + `terminal` + `search` + `web` + `skills` + `browser` + `todo` + `memory` + `session_search` + `clarify` + `code_execution` + `delegation` + `vision`) | Пакет, ориентированный на программирование, для работы с программным обеспечением: редактирование файлов, терминал, поиск, веб-документы, навыки, браузер, делегирование и выполнение кода. |
| `cronjob` | `cronjob` | Планируйте и управляйте повторяющимися задачами. |
| `debugging` | составной (`file` + `terminal` + `web`) | Пакет отладки — файл, процесс/терминал, веб-извлечение/поиск. |
| `delegation` | `delegate_task` | Создание изолированных экземпляров субагента для параллельной работы. |
| `discord` | `discord` | Базовый текст Discord/встраивание/действия DM (только для шлюза). Активен в наборе инструментов `hermes-discord`. |
| `discord_admin` | `discord_admin` | Модерация Discord (баны, смена ролей, управление каналом). Активен в наборе инструментов `hermes-discord`; требует, чтобы бот имел соответствующие разрешения Discord. |
| `feishu_doc` | `feishu_doc_read` | Прочтите содержание документа Feishu/Lark. Используется обработчиком интеллектуальных ответов Feishu на комментарии к документам. |
| `feishu_drive` | `feishu_drive_add_comment`, `feishu_drive_list_comments`, `feishu_drive_list_comment_replies`, `feishu_drive_reply_comment` | Feishu/Lark управляет операциями по комментированию. Охватывается агентом комментариев; не отображается в `hermes-cli` или других наборах инструментов обмена сообщениями. |
| `file` | `patch`, `read_file`, `search_files`, `write_file` | Чтение, запись, поиск и редактирование файлов. |
| `homeassistant` | `ha_call_service`, `ha_get_state`, `ha_list_entities`, `ha_list_services` | Управление умным домом через Home Assistant. Доступно только в том случае, если установлен `HASS_TOKEN`. |
| `computer_use` | `computer_use` | Фоновое управление рабочим столом через cua-драйвер — не крадет курсор/фокус. Работает с любой моделью, поддерживающей инструмент. macOS, Windows и Linux; требуется `cua-driver` на `$PATH`. |
| `context_engine` | (варьируется) | Инструменты времени выполнения, предоставляемые активным плагином контекстного механизма (пустые, пока плагин не заполнит их). |
| `image_gen` | `image_generate` | Генерация текста в изображение через FAL.ai (с поддержкой OpenAI/xAI). |
| `video_gen` | `video_generate`, `xai_video_edit`, `xai_video_extend` | Преобразование текста в видео и изображения в видео через бэкэнды, зарегистрированные в плагинах (xAI Grok-Imagine, FAL.ai Veo 3.1 / Pixverse v6 / Kling O3). Передайте `image_url`, чтобы анимировать изображение; опустите его для преобразования текста в видео. `xai_video_edit` / `xai_video_extend` — это инструменты редактирования/расширения, специфичные для конкретного поставщика, основанные на учетных данных xAI Imagine. |
| `kanban` | `kanban_attach`, `kanban_attach_url`, `kanban_attachments`, `kanban_block`, `kanban_comment`, `kanban_complete`, `kanban_create`, `kanban_heartbeat`, `kanban_link`, `kanban_list`, `kanban_request_changes`, `kanban_request_review`, `kanban_show`, `kanban_unblock` | Инструменты мультиагентной координации. Зарегистрировано для рабочих задач, создаваемых диспетчером (`HERMES_KANBAN_TASK`), а также для профилей, в которых набор инструментов `kanban` явно указан по имени (подстановочный знак `all`/`*` **не** включает его). Сотрудники отмечают выполненные задачи, запрашивают первоклассную проверку, блокируют, контролируют, комментируют и создают/связывают последующие задачи; Профили оркестратора дополнительно получают инструменты маршрутизации доски, такие как список/разблокировка. Дети `delegate_task` не являются владельцами запуска Канбана: их схема удаляет/отключает этот набор инструментов, а средства защиты во время выполнения отвергают прямые мутации доски, даже если присутствуют родительские переменные окружения `HERMES_KANBAN_*`. |
| `memory` | `memory` | Постоянное управление межсессионной памятью. |
| `desktop_ui` | `annotate_preview`, `close_preview`, `close_terminal`, `drive_preview`, `focus_pane`, `open_preview`, `react_to_message`, `read_preview`, `read_terminal`, `read_window_below`, `tour` | Возможности, которые действуют на рабочем столе Hermes

Само приложение p — чтение/закрытие встроенной панели терминала, открытие, чтение, закрытие, взаимодействие с браузером в приложении и добавление аннотаций, определение окна ОС за приложением, раскрытие панели, реакция на сообщение, запуск экскурсии (выделение + озвучивание элементов пользовательского интерфейса в приложении или на панели предварительного просмотра). Включено для сеансов, источником которых является настольное приложение, независимо от того, к какой серверной части оно подключено (локальное, SSH, URL-адрес или облако Hermes). Никогда не присутствует в сеансах CLI, TUI, обмена сообщениями или cron. |
| `project` | `project_create`, `project_list`, `project_switch` | Создайте и переключите рабочий стол [Проекты](../user-guide/cli.md) (именованные рабочие области с несколькими папками). Только сеансы графического интерфейса/рабочего стола. |
| `safe` | `image_generate`, `vision_analyze`, `web_extract`, `web_search` (через `includes`) | Исследования только для чтения + создание медиа. Ни запись в файл, ни терминал, ни выполнение кода. |
| `search` | `web_search` | Только веб-поиск (без извлечения). |
| `session_search` | `session_search` | Поиск прошлых сеансов беседы. |
| `skills` | `skill_manage`, `skill_view`, `skills_list` | Навыки CRUD и просмотра. |
| `spotify` | `spotify_albums`, `spotify_devices`, `spotify_library`, `spotify_playback`, `spotify_playlists`, `spotify_queue`, `spotify_search` | Встроенное управление Spotify (воспроизведение, очередь, поиск, плейлисты, альбомы, библиотека). Зарегистрировано встроенным плагином `spotify`. |
| `terminal` | `process`, `terminal` | Выполнение команд оболочки и управление фоновыми процессами. |
| `todo` | `todo` | Управление списком задач внутри сеанса. |
| `tts` | `text_to_speech` | Преобразование текста в речь. |
| `vision` | `vision_analyze` | Анализ изображений с помощью моделей с возможностью машинного зрения. |
| `video` | `video_analyze` | Инструменты анализа и понимания видео (включены, не входят в набор инструментов по умолчанию — добавьте явно через `--toolsets`). |
| `web` | `web_extract`, `web_search` | Поиск в Интернете и извлечение содержимого страниц. |
| `x_search` | `x_search` | Общедоступное обнаружение X только для чтения с помощью встроенного в xAI инструмента `x_search` Responses. Используйте навык `xurl` для аутентифицированного чтения X API и действий с учетной записью. По умолчанию выключено; зарегистрируйтесь через `hermes tools`. Схема регистрируется только тогда, когда настроены учетные данные xAI (SuperGrok OAuth или `XAI_API_KEY`). |
| `yuanbao` | `yb_query_group_info`, `yb_query_group_members`, `yb_search_sticker`, `yb_send_dm`, `yb_send_sticker` | Yuanbao DM/групповые действия и поиск стикеров. Зарегистрировано только `hermes-yuanbao`. |

## Наборы инструментов платформы

Наборы инструментов платформы определяют полную конфигурацию инструмента для цели развертывания. Большинство платформ обмена сообщениями используют тот же набор, что и `hermes-cli`:

| Набор инструментов | Отличия от `hermes-cli` |
|---------|-------------------------------|
| `hermes-cli` | Полный набор инструментов — значение по умолчанию для интерактивных сеансов CLI. Включает файл, терминал, Интернет, браузер, память, навыки, видение, image_gen, todo, tts, делегирование, code_execution, cronjob, session_search, Clarity, Computer_use, Home Assistant и инструменты канбана (все они контролируются check_fn во время выполнения). |
| `hermes-acp` | Удаляет `clarify`, `cronjob`, `image_generate`, `text_to_speech`, `computer_use`, все четыре инструмента Home Assistant и инструменты канбана. Сосредоточен на задачах кодирования в контексте IDE. |
| `hermes-api-server` | Удаляет `clarify`, `text_to_speech`, `computer_use` и инструменты канбана. Сохраняет все остальное — подходит для программного доступа, когда взаимодействие с пользователем невозможно. |
| `hermes-cron` | То же, что и `hermes-cli`. |
| `hermes-telegram` | То же, что `hermes-cli`. |
| `hermes-discord` | Добавляет `discord` и `discord_admin` поверх `hermes-cli`. |
| `hermes-slack` | То же, что `hermes-cli`. |
| `hermes-whatsapp` | То же, что `hermes-cli`. |
| `hermes-signal` | То же, что `hermes-cli`. |
| `hermes-matrix` | То же, что `hermes-cli`. |
| `hermes-mattermost` | То же, что `hermes-cli`. |
| `hermes-email` | То же, что `hermes-cli`. |
| `hermes-sms` | То же, что и `hermes-cli`. |
| `hermes-bluebubbles` | То же, что `hermes-cli`. |
| `hermes-dingtalk` | То же, что `hermes-cli`. |
| `hermes-feishu` | Добавляет пять инструментов `feishu_doc_*`/`feishu_drive_*` (используются только обработчиком комментариев к документу, а не обычным адаптером чата). |
| `hermes-qqbot` | То же, что `hermes-cli`. |
| `hermes-wecom` | То же, что `hermes-cli`. |
| `hermes-wecom-callback` | То же, что `hermes-cli`. |
| `hermes-weixin` | То же, что `hermes-cli`. |
| `hermes-yuanbao` | Добавляет пять инструментов `yb_*` (DM/группа/стикер) поверх `hermes-cli`. |
| `hermes-homeassistant` | То же, что и `hermes-cli` (инструменты Home Assistant уже присутствуют по умолчанию и активируются, когда установлен `HASS_TOKEN`). |
| `hermes-webhook` | Ограниченно безопасное подмножество — только `web_search`, `web_extract`, `vision_analyze` и `clarify`. Запуски, запускаемые с помощью веб-перехватчика, не получают доступа к терминалу, файлу или браузеру. |
| `hermes-gateway` | Набор инструментов оркестратора внутреннего шлюза — объединение всех наборов инструментов `hermes-<platform>`; используется, когда шлюзу необходимо принять любой источник сообщений. |

## Динамические наборы инструментов

### Наборы инструментов сервера MCP

Каждый настроенный сервер MCP создает набор инструментов `mcp-<server>` во время выполнения. Например, если вы настраиваете сервер `github` MCP, создается набор инструментов `mcp-github`, содержащий все инструменты, предоставляемые сервером.

```yaml
# config.yaml
mcp_servers:
  github:
    command: npx
    args: ["-y", "@modelcontextprotocol/server-github"]
```

При этом создается набор инструментов `mcp-github`, на который вы можете ссылаться в `--toolsets` или конфигурациях платформы.

### Наборы инструментов плагина

Плагины могут регистрировать свои собственные наборы инструментов через `ctx.register_tool()` во время инициализации плагина. Они появляются рядом со встроенными наборами инструментов и могут быть включены/отключены таким же образом.

### Пользовательские наборы инструментов

Определите пользовательские наборы инструментов в `config.yaml` для создания пакетов для конкретного проекта:

```yaml
toolsets:
  - hermes-cli
custom_toolsets:
  data-science:
    - file
    - terminal
    - code_execution
    - web
    - vision
```

### Подстановочные знаки

- `all` или `*` — распространяется на каждый зарегистрированный набор инструментов (встроенный + динамический + плагин).

Некоторые инструменты имеют дополнительную проверку доступности помимо членства в наборе инструментов и **не** включены только `all`/`*`:

- Инструменты **с ограниченными возможностями** (браузер, `computer_use`, `code_execution`, Feishu, Home Assistant, cronjob) появляются только тогда, когда настроены необходимые для их серверной части/учетных данных данные.
- Инструменты **закрытые для рабочего процесса** — набор инструментов `kanban` — включены намеренно. `all`/`*` **не** включает канбан; вы должны явно указать `kanban` (или быть рабочим, порожденным диспетчером, с установленным `HERMES_KANBAN_TASK`). Инструменты Канбана изменяют состояние общей доски, поэтому по умолчанию они остаются выключенными даже при `all`.

## Связь с `hermes tools`

Команда `hermes tools` предоставляет пользовательский интерфейс на основе проклятий для включения или выключения отдельных инструментов для каждой платформы. Это работает на уровне инструмента (более тонком, чем наборы инструментов) и сохраняется до `config.yaml`. Отключенные инструменты отфильтровываются, даже если их набор инструментов включен.

См. также: [Справочник по инструментам](./tools-reference.md) для получения полного списка отдельных инструментов и их параметров.