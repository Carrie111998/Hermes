# Runtime dependencies for the 모아 KakaoTalk legal bot.
# Pinned exactly — a bot that answers legal questions should not change
# its transitive dependency tree on a redeploy.
fastapi==0.133.1
uvicorn[standard]==0.41.0
starlette==1.3.1
httpx==0.28.1
python-multipart==0.0.32   # the admin review form posts multipart/form-data

# Optional, only for `ingest.py --` on PDF source material.
# pypdf==6.1.1
