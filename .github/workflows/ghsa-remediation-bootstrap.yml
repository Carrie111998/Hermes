name: GHSA remediation artifact

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: ghsa-remediation-artifact-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  remediate:
    if: >-
      github.event.pull_request.head.repo.full_name == 'andrexibiza/hermes-agent' &&
      github.event.pull_request.head.ref == 'security/ghsa-remediation-c47f0b4'
    name: Regenerate and verify dependency remediation
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Reset worktree to exact upstream base
        run: |
          set -euo pipefail
          git remote add upstream https://github.com/NousResearch/hermes-agent.git
          git fetch --no-tags upstream main
          git reset --hard "${{ github.event.pull_request.base.sha }}"
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.base.sha }}"

      - name: Set up Node 26
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 26
          cache: npm

      - name: Install npm 12
        run: npm --version | grep -q '^12\.' || npm install --global npm@12

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # 8.2.0
        with:
          version: '0.9.28'
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Apply manifest and packaged-layout corrections
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path

          ELECTRON = '41.10.3'
          NANOID = '3.3.18'

          def load(path: str):
              return json.loads(Path(path).read_text(encoding='utf-8'))

          def write(path: str, data):
              Path(path).write_text(
                  json.dumps(data, indent=2, ensure_ascii=False) + '\n',
                  encoding='utf-8',
              )

          root = load('package.json')
          root['overrides']['nanoid@^3'] = NANOID
          for key in list(root['allowScripts']):
              if key.startswith('electron@'):
                  root['allowScripts'].pop(key)
          root['allowScripts'][f'electron@{ELECTRON}'] = True
          write('package.json', root)

          website = load('website/package.json')
          website['overrides']['nanoid'] = NANOID
          write('website/package.json', website)

          desktop = load('apps/desktop/package.json')
          desktop['devDependencies']['electron'] = ELECTRON
          desktop['build']['electronVersion'] = ELECTRON
          write('apps/desktop/package.json', desktop)

          npmrc = Path('.npmrc')
          text = npmrc.read_text(encoding='utf-8')
          old = '# nanoid 3.3.17 includes fixes for GHSA-2v37-7h3g-55p8. remove when > 2wks old (rel 2026-08-03)'
          new = '# nanoid 3.3.18 includes fixes for GHSA-2v37-7h3g-55p8. remove when > 2wks old (rel 2026-08-07)'
          if old in text:
              text = text.replace(old, new, 1)
          elif new not in text:
              raise SystemExit('Nano ID release-age exception anchor not found')
          npmrc.write_text(text, encoding='utf-8')

          Path('apps/desktop/scripts/packaged-app-layout.mjs').write_text(
              "export function resolveLinuxUnpackedDirName(arch) {\n"
              "  return arch === 'x64' ? 'linux-unpacked' : `linux-${arch}-unpacked`\n"
              "}\n",
              encoding='utf-8',
          )
          Path('apps/desktop/scripts/packaged-app-layout.test.mjs').write_text(
              "import assert from 'node:assert/strict'\n"
              "import { test } from 'vitest'\n\n"
              "import { resolveLinuxUnpackedDirName } from './packaged-app-layout.mjs'\n\n"
              "test('uses electron-builder default directory on Linux x64', () => {\n"
              "  assert.equal(resolveLinuxUnpackedDirName('x64'), 'linux-unpacked')\n"
              "})\n\n"
              "test('includes architecture in electron-builder Linux ARM64 directory', () => {\n"
              "  assert.equal(resolveLinuxUnpackedDirName('arm64'), 'linux-arm64-unpacked')\n"
              "})\n",
              encoding='utf-8',
          )

          target = Path('apps/desktop/scripts/test-desktop.mjs')
          text = target.read_text(encoding='utf-8')
          anchor = "import PACKAGE_JSON from '../package.json' with { type: 'json' }"
          helper_import = "import { resolveLinuxUnpackedDirName } from './packaged-app-layout.mjs'"
          if helper_import not in text:
              if anchor not in text:
                  raise SystemExit('test-desktop import anchor not found')
              text = text.replace(anchor, helper_import + '\n' + anchor, 1)
          old_layout = "  // linux unpacked layout matches windows but with different binary name\n  const unpacked = path.join(RELEASE_ROOT, 'linux-unpacked')"
          new_layout = "  // electron-builder includes the architecture suffix for non-x64 Linux output.\n  const unpacked = path.join(RELEASE_ROOT, resolveLinuxUnpackedDirName(ARCH))"
          if old_layout in text:
              text = text.replace(old_layout, new_layout, 1)
          elif new_layout not in text:
              raise SystemExit('test-desktop Linux layout anchor not found')
          target.write_text(text, encoding='utf-8')
          PY

      - name: Regenerate exact lock graphs
        run: |
          set -euo pipefail
          npm install --package-lock-only --ignore-scripts --no-audit --no-fund
          npm update nanoid --package-lock-only --ignore-scripts --no-audit --no-fund --workspaces --include-workspace-root
          npm --prefix website install --package-lock-only --ignore-scripts --no-audit --no-fund
          npm --prefix website update nanoid --package-lock-only --ignore-scripts --no-audit --no-fund
          uv lock --upgrade-package h2

      - name: Install exact Node graphs
        run: |
          set -o pipefail
          npm ci --no-audit --no-fund | tee /tmp/npm-ci-root.txt
          npm --prefix website ci --no-audit --no-fund | tee /tmp/npm-ci-website.txt

      - name: Verify resolved security floors
        run: |
          set -o pipefail
          uv lock --check | tee /tmp/uv-lock-check.txt
          node <<'NODE'
          const root = require('./package.json')
          const desktop = require('./apps/desktop/package.json')
          const lock = require('./package-lock.json')
          const website = require('./website/package.json')
          const websiteLock = require('./website/package-lock.json')

          if (root.overrides['nanoid@^3'] !== '3.3.18') throw new Error('root Nano ID override drifted')
          if (website.overrides.nanoid !== '3.3.18') throw new Error('website Nano ID override drifted')
          if (desktop.devDependencies.electron !== '41.10.3') throw new Error('desktop Electron dependency drifted')
          if (desktop.build.electronVersion !== '41.10.3') throw new Error('electron-builder runtime drifted')
          if (!root.allowScripts['electron@41.10.3']) throw new Error('Electron lifecycle allowlist drifted')

          const electron = lock.packages['node_modules/electron'] || lock.packages['apps/desktop/node_modules/electron']
          if (!electron || electron.version !== '41.10.3') throw new Error(`Electron lock resolved ${electron?.version}`)

          let rootNanoid3 = 0
          for (const [path, pkg] of Object.entries(lock.packages)) {
            if (path.endsWith('/nanoid') && pkg.version?.startsWith('3.')) {
              rootNanoid3 += 1
              if (pkg.version !== '3.3.18') throw new Error(`${path} resolves Nano ID ${pkg.version}`)
            }
            if (path.endsWith('/extract-zip') && pkg.version === '2.0.1') {
              throw new Error(`${path} still resolves extract-zip 2.0.1`)
            }
          }
          if (rootNanoid3 === 0) throw new Error('no root Nano ID v3 resolution inspected')

          let websiteNanoid3 = 0
          for (const [path, pkg] of Object.entries(websiteLock.packages)) {
            if (path.endsWith('/nanoid') && pkg.version?.startsWith('3.')) {
              websiteNanoid3 += 1
              if (pkg.version !== '3.3.18') throw new Error(`website ${path} resolves Nano ID ${pkg.version}`)
            }
          }
          if (websiteNanoid3 === 0) throw new Error('no website Nano ID v3 resolution inspected')
          NODE
          python3 - <<'PY'
          import tomllib
          from pathlib import Path

          data = tomllib.loads(Path('uv.lock').read_text(encoding='utf-8'))
          versions = [package['version'] for package in data['package'] if package['name'] == 'h2']
          if versions != ['4.4.1']:
              raise SystemExit(f'h2 lock resolved {versions!r}, expected 4.4.1')
          PY

      - name: Verify dependency audits
        run: |
          set -o pipefail
          npm audit --workspaces=false --audit-level=moderate | tee /tmp/npm-audit-root.txt
          npm audit --workspace web --audit-level=moderate | tee /tmp/npm-audit-web.txt
          npm audit --workspace ui-tui --audit-level=moderate | tee /tmp/npm-audit-tui.txt
          npm audit --workspace apps/desktop --audit-level=moderate | tee /tmp/npm-audit-desktop.txt
          npm --prefix website audit --audit-level=moderate | tee /tmp/npm-audit-website.txt

      - name: Run affected repository gates
        run: |
          set -o pipefail
          npm run build --workspace web | tee /tmp/web-build.txt
          npm run check --workspace ui-tui | tee /tmp/tui-check.txt
          npm run check:lint --workspace apps/desktop | tee /tmp/desktop-lint.txt
          npm run test:desktop:platforms --workspace apps/desktop | tee /tmp/desktop-platform-tests.txt
          npm run build --workspace apps/desktop | tee /tmp/desktop-build.txt
          git diff --check

      - name: Build exact-object verification receipt
        run: |
          mkdir -p /tmp/ghsa-remediation
          {
            echo "source_head=${{ github.event.pull_request.head.sha }}"
            echo "upstream_base=${{ github.event.pull_request.base.sha }}"
            echo "node=$(node --version)"
            echo "npm=$(npm --version)"
            echo "uv=$(uv --version)"
            echo "verified_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo "all_dependency_and_affected_repository_gates=passed"
          } > /tmp/ghsa-remediation/verification.txt
          cp /tmp/uv-lock-check.txt /tmp/npm-ci-root.txt /tmp/npm-ci-website.txt \
            /tmp/npm-audit-root.txt /tmp/npm-audit-web.txt /tmp/npm-audit-tui.txt \
            /tmp/npm-audit-desktop.txt /tmp/npm-audit-website.txt \
            /tmp/web-build.txt /tmp/tui-check.txt /tmp/desktop-lint.txt \
            /tmp/desktop-platform-tests.txt /tmp/desktop-build.txt \
            /tmp/ghsa-remediation/

      - name: Upload verified remediation files
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ghsa-remediation-files
          path: |
            .npmrc
            package.json
            package-lock.json
            website/package.json
            website/package-lock.json
            apps/desktop/package.json
            apps/desktop/scripts/test-desktop.mjs
            apps/desktop/scripts/packaged-app-layout.mjs
            apps/desktop/scripts/packaged-app-layout.test.mjs
            uv.lock
            /tmp/ghsa-remediation
          retention-days: 1
          include-hidden-files: true
          if-no-files-found: error
