name: Materialize typed process-edge authority

on:
  push:
    branches:
      - security/process-edge-authority-broker

permissions:
  contents: write

concurrency:
  group: materialize-process-edge-authority
  cancel-in-progress: false

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Materialize reviewed source object
        run: python .github/materialize-process-edge-authority.py

      - name: Compile changed Python surfaces
        run: |
          python -m compileall -q \
            tools/child_process_authority.py \
            tools/environments/base.py \
            tools/environments/singularity.py \
            tools/checkpoint_manager.py \
            agent/secret_sources/bitwarden.py \
            agent/secret_sources/onepassword.py \
            agent/secret_sources/command.py \
            agent/transports/codex_app_server.py \
            agent/copilot_acp_client.py \
            hermes_cli/pty_bridge.py \
            hermes_cli/win_pty_bridge.py \
            hermes_cli/secrets_cli.py \
            hermes_cli/onepassword_secrets_cli.py \
            tui_gateway/host_supervisor.py \
            tui_gateway/methods_tools.py \
            tests/tools/test_child_process_authority.py \
            tests/security/test_child_process_authority_guard.py

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # 8.2.0
        with:
          version: "0.9.28"
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Install Python and focused test environment
        run: |
          uv python install 3.11
          uv sync --locked --python 3.11 --extra dev

      - name: Run authority and affected regression suites
        run: |
          source .venv/bin/activate
          python -m pytest -q \
            tests/tools/test_child_process_authority.py \
            tests/security/test_child_process_authority_guard.py \
            tests/test_onepassword_secrets.py \
            tests/test_env_loader_secret_sources.py \
            tests/tools/test_checkpoint_manager.py

      - name: Lint changed Python surfaces
        run: |
          source .venv/bin/activate
          ruff check \
            tools/child_process_authority.py \
            tools/environments/base.py \
            tools/environments/singularity.py \
            tools/checkpoint_manager.py \
            agent/secret_sources/bitwarden.py \
            agent/secret_sources/onepassword.py \
            agent/secret_sources/command.py \
            agent/transports/codex_app_server.py \
            agent/copilot_acp_client.py \
            hermes_cli/pty_bridge.py \
            hermes_cli/win_pty_bridge.py \
            hermes_cli/secrets_cli.py \
            hermes_cli/onepassword_secrets_cli.py \
            tui_gateway/host_supervisor.py \
            tui_gateway/methods_tools.py \
            tests/tools/test_child_process_authority.py \
            tests/security/test_child_process_authority_guard.py
          ruff format --check \
            tools/child_process_authority.py \
            tests/tools/test_child_process_authority.py \
            tests/security/test_child_process_authority_guard.py
          git diff --check

      - name: Publish one clean review object
        env:
          BASE_SHA: bdf10471b5dff0d67d172065f7dbf9f4c47c4c1b
          BRANCH: security/process-edge-authority-broker
        run: |
          rm -f \
            .github/materialize-process-edge-authority.py \
            .github/workflows/materialize-process-edge-authority.yml
          git add -A
          git config user.name "Andrex Ibiza, MBA"
          git config user.email "andrexibiza@gmail.com"
          git reset --soft "$BASE_SHA"
          git commit -m "feat(security): bind child process authority to typed edges"
          git push --force-with-lease origin "HEAD:$BRANCH"
