# The Compresr Hermes integration lives in the `compresr` PyPI package
# (module `compresr.integrations.hermes`). Install it into the SAME
# interpreter that runs Hermes, then restart:
#
#     pip install 'compresr>=2.8.4'
#
# Hermes does not auto-install repo-plugin dependencies; this file documents
# the runtime requirement for operators and CI. Pin is the first release that
# ships compresr.integrations.hermes (current PyPI 2.8.2 does not).
compresr>=2.8.4
