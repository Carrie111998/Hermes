name: web-tinyfish
version: 1.0.0
description: "TinyFish web search + fetch — free Search and Fetch APIs. Requires TINYFISH_API_KEY (free signup at https://agent.tinyfish.ai/api-keys)."
author: NousResearch
kind: backend
provides_web_providers:
  - tinyfish
requires_env:
  - TINYFISH_API_KEY