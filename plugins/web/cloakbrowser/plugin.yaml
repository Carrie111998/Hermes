name: web-cloakbrowser
version: 1.0.0
description: "Stealth Chromium web search + extract via CloakBrowser (DuckDuckGo HTML + page crawl). No API key."
author: zapabob + Hermes Agent
kind: backend
provides_web_providers:
  - cloakbrowser
mcp_servers:
  - name: cloakbrowser
    module: plugins.web.cloakbrowser.mcp_server

